# Secrets — never commit
.env
.env.*
.env.local

# Allow the example file — it contains no real secrets, only placeholder values
!.env.example

# Generated data files — conversations.db lives on Railway Volume; never commit it.
# FAISS index, metadata, and CSV are committed so Railway can clone them at deploy.
data/conversations.db

# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/

# OS
.DS_Store
Thumbs.db
