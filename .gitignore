# Secrets — never commit
.env
.env.*
.env.local

# Allow the example file — it contains no real secrets, only placeholder values
!.env.example

# Generated data files — gitignored because they are large and regenerable
data/faiss.index
data/metadata.json
data/experts.csv

# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/

# OS
.DS_Store
Thumbs.db
