# Backend API base URL (no trailing slash)
# Development: point to local FastAPI server
# Production: Railway deployment URL
VITE_API_URL=http://localhost:8000

# Intercom app ID for the floating support widget (public pages only)
# Defaults to the hardcoded value if not set
VITE_INTERCOM_APP_ID=o9v3tocw
