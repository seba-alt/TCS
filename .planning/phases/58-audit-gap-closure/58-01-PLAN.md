---
phase: 58-audit-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/main.py
  - frontend/src/components/marketplace/FilterChips.tsx
  - frontend/src/components/sidebar/RateSlider.tsx
  - frontend/src/components/marketplace/FilterChips.test.ts
autonomous: true
requirements: [BUG-02, ADM-06]
gap_closure: true

must_haves:
  truths:
    - "An admin user can send a DELETE request to /api/admin/experts/{username} without a CORS preflight error"
    - "The rate filter chip in FilterChips displays currency symbol from currencySymbol('EUR'), not a hardcoded euro sign"
    - "The RateSlider labels display currency symbol from currencySymbol('EUR'), not a hardcoded euro sign"
    - "All existing frontend tests pass including the new currency symbol test"
  artifacts:
    - path: "app/main.py"
      provides: "CORS middleware with DELETE in allow_methods"
      contains: 'allow_methods=["GET", "POST", "DELETE"]'
    - path: "frontend/src/components/marketplace/FilterChips.tsx"
      provides: "Rate chip label using currencySymbol utility"
      contains: "currencySymbol"
    - path: "frontend/src/components/sidebar/RateSlider.tsx"
      provides: "Slider labels using currencySymbol utility"
      contains: "currencySymbol"
    - path: "frontend/src/components/marketplace/FilterChips.test.ts"
      provides: "Test confirming currencySymbol utility is used for rate chip"
      contains: "currencySymbol"
  key_links:
    - from: "FilterChips.tsx"
      to: "frontend/src/utils/currency.ts"
      via: "import { currencySymbol }"
      pattern: "import.*currencySymbol.*currency"
    - from: "RateSlider.tsx"
      to: "frontend/src/utils/currency.ts"
      via: "import { currencySymbol }"
      pattern: "import.*currencySymbol.*currency"
---

<objective>
Fix CORS DELETE rejection and adopt the currencySymbol() utility in the two remaining filter components that still hardcode the euro sign.

Purpose: Close the two code-level gaps from the v5.0 milestone audit — admin expert deletion fails silently due to CORS, and two filter surfaces bypass the currency utility.
Output: CORS permits DELETE, FilterChips and RateSlider use currencySymbol(), test extended.
</objective>

<execution_context>
@/Users/sebastianhamers/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sebastianhamers/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-audit-gap-closure/58-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CORS DELETE fix and currencySymbol adoption in FilterChips + RateSlider</name>
  <files>app/main.py, frontend/src/components/marketplace/FilterChips.tsx, frontend/src/components/sidebar/RateSlider.tsx</files>
  <action>
Three targeted edits:

1. **app/main.py** line 323 — change `allow_methods=["GET", "POST"]` to `allow_methods=["GET", "POST", "DELETE"]`. Do NOT add `"*"` or any other methods. Do NOT touch `allow_credentials` or `allow_headers`.

2. **frontend/src/components/marketplace/FilterChips.tsx** — add `import { currencySymbol } from '../../utils/currency'` at the top imports. On line 24, replace the hardcoded euro template literal:
   - Before: `` label: `€${rateMin}–€${rateMax}`, ``
   - After: `` label: `${currencySymbol('EUR')}${rateMin}–${currencySymbol('EUR')}${rateMax}`, ``

3. **frontend/src/components/sidebar/RateSlider.tsx** — add `import { currencySymbol } from '../../utils/currency'` at the top imports. Add a const `const sym = currencySymbol('EUR')` near the top of the component function body. Replace the three hardcoded euro literals:
   - Line 50: `<span>€{localValue[0]}</span>` → `<span>{sym}{localValue[0]}</span>`
   - Line 51: `<span>€{localValue[1]}</span>` → `<span>{sym}{localValue[1]}</span>`
   - Line 81: `€{roundedMax}/hr max` → `{sym}{roundedMax}/hr max`

Do NOT add PATCH or any other methods to CORS. Do NOT change the import path — both files use `'../../utils/currency'` (verified: both are 2 directories deep from src/).
  </action>
  <verify>
    <automated>cd /Users/sebastianhamers/Documents/TCS/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Verify CORS line reads ["GET", "POST", "DELETE"] in app/main.py</manual>
  </verify>
  <done>app/main.py allows DELETE in CORS, FilterChips.tsx and RateSlider.tsx import and use currencySymbol('EUR') instead of hardcoded €, TypeScript compiles without errors</done>
</task>

<task type="auto">
  <name>Task 2: Extend FilterChips.test.ts with currency symbol coverage</name>
  <files>frontend/src/components/marketplace/FilterChips.test.ts</files>
  <action>
Extend the existing test file `frontend/src/components/marketplace/FilterChips.test.ts` by adding a new `describe` block at the end of the file (after the existing `describe('FilterChips rate chip visibility', ...)` block).

Add `import { currencySymbol } from '../../utils/currency'` to the existing imports at the top.

New describe block:

```typescript
describe('FilterChips rate chip currency symbol', () => {
  it('uses currencySymbol utility, not hardcoded €', () => {
    const sym = currencySymbol('EUR')
    expect(sym).toBe('€')
  })

  it('supports other currency codes via the utility', () => {
    expect(currencySymbol('USD')).toBe('$')
    expect(currencySymbol('GBP')).toBe('£')
  })
})
```

This proves the utility is wired and returns correct symbols — the adoption in FilterChips.tsx and RateSlider.tsx is confirmed by the TypeScript compilation in Task 1 and by grep for `currencySymbol` in those files.
  </action>
  <verify>
    <automated>cd /Users/sebastianhamers/Documents/TCS/frontend && npm test 2>&1</automated>
  </verify>
  <done>All tests pass including the new currency symbol tests; 7+ total tests green</done>
</task>

</tasks>

<verification>
1. `grep -n "allow_methods" app/main.py` shows `["GET", "POST", "DELETE"]`
2. `grep -n "currencySymbol" frontend/src/components/marketplace/FilterChips.tsx` shows import + usage
3. `grep -n "currencySymbol" frontend/src/components/sidebar/RateSlider.tsx` shows import + usage
4. `grep -c "€" frontend/src/components/marketplace/FilterChips.tsx` returns 0 (no hardcoded euros)
5. `grep -c "€" frontend/src/components/sidebar/RateSlider.tsx` returns 0 (no hardcoded euros)
6. `cd frontend && npm test` passes with all tests green
</verification>

<success_criteria>
- CORS middleware allows GET, POST, and DELETE (not wildcard)
- FilterChips.tsx uses currencySymbol('EUR') for rate chip label
- RateSlider.tsx uses currencySymbol('EUR') for all three label positions
- No hardcoded € characters remain in FilterChips.tsx or RateSlider.tsx
- All frontend tests pass including the new currency symbol describe block
</success_criteria>

<output>
After completion, create `.planning/phases/58-audit-gap-closure/58-01-SUMMARY.md`
</output>
