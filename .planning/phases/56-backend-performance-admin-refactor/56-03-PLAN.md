---
phase: 56-backend-performance-admin-refactor
plan: 03
type: execute
wave: 2
depends_on:
  - 56-01
  - 56-02
files_modified:
  - app/routers/admin.py
  - app/routers/admin/__init__.py
  - app/routers/admin/_common.py
  - app/routers/admin/analytics.py
  - app/routers/admin/experts.py
  - app/routers/admin/leads.py
  - app/routers/admin/exports.py
  - app/routers/admin/settings.py
  - app/routers/admin/events.py
autonomous: true
requirements:
  - ADM-01

must_haves:
  truths:
    - "main.py can import auth_router and router from app.routers.admin without changes to main.py import lines"
    - "All /api/admin/* endpoints respond with the same status codes and behavior as before the split"
    - "Each admin sub-module file is independently readable at under 400 lines"
    - "The settings POST endpoint calls invalidate_settings_cache() after writing to the database"
    - "Expert write paths (add_expert, tag-all, ingest, import-csv) call sync_expert_tags() after updating tags"
  artifacts:
    - path: "app/routers/admin/__init__.py"
      provides: "Package assembly, re-exports auth_router, router, _auto_categorize"
      contains: "__all__"
    - path: "app/routers/admin/_common.py"
      provides: "Shared auth dep, helpers, constants, Pydantic models, ingest state"
      contains: "_require_admin"
    - path: "app/routers/admin/analytics.py"
      provides: "Stats, searches, gaps endpoints"
      contains: "router"
    - path: "app/routers/admin/experts.py"
      provides: "Expert CRUD, tag-all, ingest, domain-map, compare endpoints"
      contains: "router"
    - path: "app/routers/admin/leads.py"
      provides: "Leads, newsletter endpoints"
      contains: "router"
    - path: "app/routers/admin/exports.py"
      provides: "CSV export endpoints"
      contains: "router"
    - path: "app/routers/admin/settings.py"
      provides: "Settings GET+POST, reset-data endpoint"
      contains: "invalidate_settings_cache"
    - path: "app/routers/admin/events.py"
      provides: "Events demand/exposure, analytics-summary, lead-clicks endpoints"
      contains: "router"
  key_links:
    - from: "app/routers/admin/__init__.py"
      to: "app/routers/admin/_common.py"
      via: "import and re-export of auth_router, router, _auto_categorize"
      pattern: "from app\\.routers\\.admin\\._common import"
    - from: "app/routers/admin/settings.py"
      to: "app/services/search_intelligence.py"
      via: "invalidate_settings_cache() call after settings POST"
      pattern: "invalidate_settings_cache"
    - from: "app/routers/admin/experts.py"
      to: "app/services/tag_sync.py"
      via: "sync_expert_tags() calls in write paths"
      pattern: "sync_expert_tags"
    - from: "main.py"
      to: "app/routers/admin/__init__.py"
      via: "Unchanged import: from app.routers import admin"
      pattern: "from app\\.routers import admin"
---

<objective>
Split the 2,225-line admin.py monolith into 6 logical sub-modules within an admin/ package, wire the settings cache invalidation from Plan 01, and wire the tag sync calls from Plan 02 into expert write paths.

Purpose: The admin router is the largest file in the codebase and difficult to read, navigate, or modify. Splitting into focused modules enables independent editing and faster Claude context loading. This plan also wires the cross-cutting concerns from Plans 01 and 02 that touch admin endpoints.

Output: `app/routers/admin/` package with `__init__.py`, `_common.py`, and 6 route modules. The original `admin.py` file is deleted. All endpoints work identically. Settings cache invalidation and tag sync are wired.
</objective>

<execution_context>
@/Users/sebastianhamers/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sebastianhamers/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/56-backend-performance-admin-refactor/56-RESEARCH.md
@.planning/phases/56-backend-performance-admin-refactor/56-01-SUMMARY.md
@.planning/phases/56-backend-performance-admin-refactor/56-02-SUMMARY.md
@app/routers/admin.py
@main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create admin package with _common.py and __init__.py</name>
  <files>app/routers/admin/__init__.py, app/routers/admin/_common.py</files>
  <action>
**Step 1: Create the admin/ package directory**

Create `app/routers/admin/` directory.

**Step 2: Create _common.py with shared code**

Extract from the current `admin.py` (lines 1-334 per research) into `app/routers/admin/_common.py`:

1. **All imports** from the top of admin.py (lines 1-68). Keep only imports that are actually used by the shared code or will be needed by multiple sub-modules. Sub-modules will import what they need independently.

2. **Ingest job state and helpers** (lines 69-164): The global `_ingest` dict, `_run_tag_job`, `_run_ingest_job` functions. These are used by experts.py but defined here because they're shared state.

3. **Auth dependency and helpers** (lines 165-282): `_require_admin`, `_auto_categorize`, `_serialize_expert`, and any other helper functions used across multiple sub-modules.

4. **auth_router** (lines 283-334): The `auth_router = APIRouter(prefix="/api/admin")` with the `/auth` login endpoint and the `/lead-clicks` POST endpoint. This router has NO `_require_admin` dependency (login must work without auth).

5. **router** definition: `router = APIRouter(prefix="/api/admin", dependencies=[Depends(_require_admin)])`. This is the main authenticated router. Do NOT add any routes to it in `_common.py` — sub-modules add their routes to their own local `APIRouter()` instances, and `__init__.py` assembles them via `router.include_router()`.

**Critical constraints:**
- `_common.py` must NOT import from sibling sub-modules (no circular imports)
- Sub-modules import from `_common` using `from app.routers.admin._common import ...`
- `auth_router` keeps its `prefix="/api/admin"` because it's included directly in main.py
- `router` keeps its `prefix="/api/admin"` and `dependencies=[Depends(_require_admin)]`

**Step 3: Create __init__.py**

Create `app/routers/admin/__init__.py` that:
1. Imports `auth_router`, `router`, `_auto_categorize` from `_common`
2. Imports all 6 sub-modules (analytics, experts, leads, exports, settings, events)
3. Calls `router.include_router(submodule.router)` for each sub-module
4. Sets `__all__ = ["auth_router", "router", "_auto_categorize"]`

Sub-module routers are plain `APIRouter()` with NO prefix — they inherit `/api/admin` from the parent `router`.

Reference the exact pattern from 56-RESEARCH.md "Admin Package __init__.py Assembly" and "Recommended Admin Module Structure".

**Note:** The 6 sub-module files don't exist yet — Task 2 creates them. The __init__.py imports will fail until Task 2 completes, but write the file now with the complete structure. Task 2 creates the sub-modules so everything connects.
  </action>
  <verify>
    <automated>cd /Users/sebastianhamers/Documents/TCS && python -c "
import os
assert os.path.exists('app/routers/admin/__init__.py'), '__init__.py exists'
assert os.path.exists('app/routers/admin/_common.py'), '_common.py exists'
# Check _common.py has the key exports
with open('app/routers/admin/_common.py') as f:
    content = f.read()
assert '_require_admin' in content, '_require_admin defined'
assert 'auth_router' in content, 'auth_router defined'
assert '_auto_categorize' in content, '_auto_categorize defined'
print('PASS: Admin package structure created')
"</automated>
  </verify>
  <done>admin/ package exists with _common.py containing shared auth, helpers, and ingest state. __init__.py assembles sub-routers and re-exports auth_router, router, _auto_categorize for backward compatibility with main.py.</done>
</task>

<task type="auto">
  <name>Task 2: Create 6 sub-module routers and delete admin.py</name>
  <files>app/routers/admin/analytics.py, app/routers/admin/experts.py, app/routers/admin/leads.py, app/routers/admin/exports.py, app/routers/admin/settings.py, app/routers/admin/events.py</files>
  <action>
Split the remaining routes from admin.py into 6 sub-modules. Each sub-module follows the same pattern:

```python
from fastapi import APIRouter
router = APIRouter()  # NO prefix, NO dependencies — inherited from parent

# Import shared items from _common
from app.routers.admin._common import _require_admin, _serialize_expert, ...

@router.get("/endpoint-path")
async def endpoint_name(...):
    ...
```

**Route assignments (from 56-RESEARCH.md route map):**

1. **analytics.py** (lines 335-529): `GET /stats`, `GET /searches`, `GET /gaps`, `POST /gaps/{id}/resolve`, `GET /analytics-summary`
   - Note: `/analytics-summary` is currently in the events section (lines 1708-1959) but logically belongs with analytics. Move it here.

2. **experts.py** (lines 764-1404 + 1960-2225): `/experts` CRUD, `/experts/tag-all`, `/ingest/*`, `/domain-map`, `/compare`, and the Search Lab (`POST /compare`)
   - **Wire sync_expert_tags:** In every endpoint that modifies Expert.tags or Expert.industry_tags, add a call to `sync_expert_tags(db, expert.id, tags_list, industry_tags_list)` from `app.services.tag_sync` BEFORE committing. The write paths are:
     - `POST /experts` (add_expert) — after setting tags on the new expert
     - `POST /experts/tag-all` — inside `_run_tag_job` after each expert's tags are updated
     - `POST /ingest/run` — inside `_run_ingest_job` after the ingest completes. Here, call `sync_all_expert_tags(db)` once at the end rather than per-expert, since it's a full rebuild.
     - `POST /experts/import-csv` — after each expert's tags are set from CSV data
   - Import `sync_expert_tags` and `sync_all_expert_tags` from `app.services.tag_sync`

3. **leads.py** (lines 530-763): `GET /leads`, `GET /newsletter-subscribers`, `GET /lead-clicks` (the GET endpoint for listing), `GET /export/leads.csv`, `GET /export/newsletter.csv`

4. **exports.py** (lines 1405-1498): `GET /export/searches.csv`, `GET /export/gaps.csv`, `GET /export/exposure.csv`

5. **settings.py** (lines 1499-1707): `GET /settings`, `POST /settings`, `POST /reset-data`
   - **Wire invalidate_settings_cache:** In the `POST /settings` endpoint, after `db.commit()`, call `invalidate_settings_cache()` from `app.services.search_intelligence`. This ensures cached settings are refreshed immediately when an admin changes them.
   - Import: `from app.services.search_intelligence import invalidate_settings_cache`

6. **events.py** (lines 1708-1959 minus analytics-summary): `GET /events/demand`, `GET /events/exposure`, `GET /lead-clicks`, `GET /lead-clicks/by-expert/{username}`

**After creating all 6 sub-modules:**

Delete the original `app/routers/admin.py` file. It is fully replaced by the `app/routers/admin/` package.

**Critical constraints:**
- Sub-module `router = APIRouter()` with NO prefix — routes keep their relative paths (e.g., `@router.get("/stats")`, not `@router.get("/api/admin/stats")`)
- Each sub-module imports what it needs from `_common` and from standard libraries independently
- No sub-module imports from another sub-module (prevents circular deps)
- `_ingest` state dict and ingest job functions stay in `_common.py` but are imported by `experts.py`
- Verify every route in the original admin.py has a home in a sub-module — no routes dropped

**Verification approach:** After the split, count the total number of `@router.` and `@auth_router.` decorators across all sub-modules + _common.py. Compare with the count in the original admin.py to ensure no routes were lost.
  </action>
  <verify>
    <automated>cd /Users/sebastianhamers/Documents/TCS && python -c "
# Verify the package imports work and main.py compatibility
from app.routers import admin
assert hasattr(admin, 'auth_router'), 'auth_router importable'
assert hasattr(admin, 'router'), 'router importable'
assert hasattr(admin, '_auto_categorize'), '_auto_categorize importable'

# Verify old admin.py is gone
import os
assert not os.path.isfile('app/routers/admin.py'), 'Old admin.py should be deleted'
assert os.path.isdir('app/routers/admin'), 'admin/ package should exist'

# Verify all 6 sub-modules exist
for mod in ['analytics', 'experts', 'leads', 'exports', 'settings', 'events']:
    assert os.path.exists(f'app/routers/admin/{mod}.py'), f'{mod}.py exists'

# Verify settings.py has invalidation wiring
with open('app/routers/admin/settings.py') as f:
    assert 'invalidate_settings_cache' in f.read(), 'Settings cache invalidation wired'

# Verify experts.py has tag sync wiring
with open('app/routers/admin/experts.py') as f:
    assert 'sync_expert_tags' in f.read(), 'Tag sync wired in experts'

print('PASS: Admin split complete, all modules importable, wiring verified')
"</automated>
    <manual>
1. Start the app: cd /Users/sebastianhamers/Documents/TCS && python -c "from main import app; print('App loads OK')"
2. Verify no 404s on key admin endpoints by checking route registration
    </manual>
  </verify>
  <done>admin.py monolith replaced by 6 focused sub-modules, each under 400 lines. All routes preserved. Settings cache invalidation wired in POST /settings. Tag sync wired in expert write paths. main.py imports unchanged. Old admin.py deleted.</done>
</task>

</tasks>

<verification>
1. `from app.routers import admin` works — `auth_router`, `router`, `_auto_categorize` importable
2. Old `admin.py` file is deleted
3. All 8 files exist in `app/routers/admin/`
4. No sub-module has `prefix=` in its `APIRouter()` constructor
5. `settings.py` calls `invalidate_settings_cache()` after POST /settings commit
6. `experts.py` calls `sync_expert_tags()` or `sync_all_expert_tags()` in all tag write paths
7. Route count matches original (no dropped endpoints)
8. Application starts without ImportError
</verification>

<success_criteria>
- Admin router split from 1 file (2,225 lines) into 8 files (each under 400 lines)
- All /api/admin/* endpoints work identically
- main.py import lines unchanged
- Settings cache invalidation wired (from Plan 01)
- Tag sync wired in expert write paths (from Plan 02)
- No circular imports between sub-modules
</success_criteria>

<output>
After completion, create `.planning/phases/56-backend-performance-admin-refactor/56-03-SUMMARY.md`
</output>
