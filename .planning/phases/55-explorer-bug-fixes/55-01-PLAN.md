---
phase: 55-explorer-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/explorer.py
  - frontend/index.html
  - frontend/public/og-image.png
autonomous: true
requirements:
  - BUG-01
  - BUG-08

must_haves:
  truths:
    - "Search results with a query return Top Match experts before Good Match experts before unscored experts"
    - "Within each tier group, experts are ranked by final_score (relevance) descending"
    - "Sharing the site URL produces a rich link preview with Tinrate title, description, and purple logo image"
  artifacts:
    - path: "app/services/explorer.py"
      provides: "Tier-grouped sorting in scored.sort()"
      contains: "tier_key"
    - path: "frontend/index.html"
      provides: "Open Graph meta tags"
      contains: "og:image"
    - path: "frontend/public/og-image.png"
      provides: "OG preview image (purple logo)"
  key_links:
    - from: "app/services/explorer.py"
      to: "ExpertCard.findability_score"
      via: "tier grouping sort key"
      pattern: "findability_score.*88|75"
---

<objective>
Fix match tier sorting in backend search results and add Open Graph meta tags for rich link previews.

Purpose: BUG-01 ensures experts are presented in tier-grouped order (Top Match > Good Match > rest) during search. BUG-08 ensures shared links to the platform render a branded rich card on social media and messaging apps.
Output: Updated explorer service with tier-sorted results; index.html with OG meta tags; OG image in public directory.
</objective>

<execution_context>
@/Users/sebastianhamers/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sebastianhamers/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-explorer-bug-fixes/55-CONTEXT.md
@app/services/explorer.py
@frontend/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement tier-grouped sorting in backend explorer</name>
  <files>app/services/explorer.py</files>
  <action>
In `app/services/explorer.py`, modify the sort on line 338 (`scored.sort(key=lambda x: x[0], reverse=True)`) to sort by tier group FIRST, then by final_score within each tier.

The tier grouping logic uses the same thresholds as `findabilityLabel()` in the frontend:
- Tier 0 (Top Match): findability_score >= 88
- Tier 1 (Good Match): findability_score >= 75
- Tier 2 (rest): findability_score < 75 or None

Create a helper function `_tier_key(findability_score: float | None) -> int` that returns 0 for Top Match, 1 for Good Match, 2 for rest.

Replace the sort with:
```python
scored.sort(key=lambda x: (_tier_key(x[3].findability_score), -x[0]))
```

This sorts by tier ascending (0 first = Top Match), then by final_score descending (negative for ascending sort).

IMPORTANT: This tier-grouped sorting ONLY applies when a search query is active (the `if query:` branch, around line 338). The pure filter mode branch (the `else:` branch starting at line 357) should remain unchanged — it already sorts by findability_score DESC or weighted-random seed.

Do NOT change the pure filter mode sorting logic.
  </action>
  <verify>
    <automated>cd /Users/sebastianhamers/Documents/TCS && python -c "
from app.services.explorer import _tier_key
assert _tier_key(95) == 0, 'Top Match should be tier 0'
assert _tier_key(88) == 0, 'Score 88 should be Top Match'
assert _tier_key(80) == 1, 'Score 80 should be Good Match'
assert _tier_key(75) == 1, 'Score 75 should be Good Match'
assert _tier_key(60) == 2, 'Score 60 should be rest'
assert _tier_key(None) == 2, 'None should be rest'
print('All tier key tests passed')
"</automated>
    <manual>Run a search query on the Explorer and verify Top Match cards appear before Good Match cards</manual>
  </verify>
  <done>Search results are sorted by tier group first (Top Match > Good Match > rest), then by final_score within each tier. Pure filter mode remains unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add Open Graph meta tags and preview image</name>
  <files>frontend/index.html, frontend/public/og-image.png</files>
  <action>
1. Copy `frontend/Logo Icon Purple.png` to `frontend/public/og-image.png` so it is served as a static asset at `/og-image.png`.

2. In `frontend/index.html`, add OG meta tags inside the `<head>` section, AFTER the `<title>` tag and BEFORE the Google Analytics script:

```html
<!-- Open Graph -->
<meta property="og:type" content="website" />
<meta property="og:title" content="Tinrate" />
<meta property="og:description" content="Describe any problem and instantly get matched with the right expert. No searching, no guesswork." />
<meta property="og:image" content="https://tcs-three-sigma.vercel.app/og-image.png" />
<meta property="og:url" content="https://tcs-three-sigma.vercel.app" />
<!-- Twitter Card -->
<meta name="twitter:card" content="summary" />
<meta name="twitter:title" content="Tinrate" />
<meta name="twitter:description" content="Describe any problem and instantly get matched with the right expert. No searching, no guesswork." />
<meta name="twitter:image" content="https://tcs-three-sigma.vercel.app/og-image.png" />
```

The description is Claude's discretion per CONTEXT.md. The image uses the production URL since OG crawlers fetch absolute URLs.
  </action>
  <verify>
    <automated>cd /Users/sebastianhamers/Documents/TCS && test -f frontend/public/og-image.png && grep -q 'og:image' frontend/index.html && grep -q 'og:title' frontend/index.html && grep -q 'og:description' frontend/index.html && echo "OG tags and image verified"</automated>
    <manual>Use https://www.opengraph.xyz/ to preview the deployed URL after shipping</manual>
  </verify>
  <done>index.html contains og:title, og:description, og:image, og:type meta tags. og-image.png exists in public directory. Twitter card meta tags included for cross-platform compatibility.</done>
</task>

</tasks>

<verification>
- `python -c "from app.services.explorer import _tier_key; print('import ok')"` succeeds
- `grep -c 'og:' frontend/index.html` returns at least 4
- `test -f frontend/public/og-image.png` succeeds
- `cd frontend && npx vite build` succeeds without errors
</verification>

<success_criteria>
1. Backend search results return Top Match experts before Good Match experts before unscored experts when a search query is active
2. Pure filter mode (no query) sorting is unchanged
3. index.html has OG meta tags with title, description, image, and type
4. Purple logo image is available at /og-image.png
5. Frontend builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/55-explorer-bug-fixes/55-01-SUMMARY.md`
</output>
