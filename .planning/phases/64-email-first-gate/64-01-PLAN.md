---
phase: 64-email-first-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/marketplace/EmailEntryGate.tsx
  - frontend/src/pages/MarketplacePage.tsx
  - frontend/src/components/marketplace/NewsletterGateModal.tsx
autonomous: true
requirements:
  - GATE-01
  - GATE-02
  - GATE-03
  - GATE-04
must_haves:
  truths:
    - "New visitor sees full-screen email gate before any expert cards are visible"
    - "Submitting a valid email dismisses the gate with a smooth fade-out revealing the Explorer"
    - "Returning subscriber sees no gate — Explorer loads directly without any flash"
    - "Gate cannot be dismissed by clicking outside, pressing Escape, or any path other than email submission"
    - "Gate submission sends source page_entry to Loops via newsletter/subscribe endpoint"
  artifacts:
    - path: "frontend/src/components/marketplace/EmailEntryGate.tsx"
      provides: "Full-screen mandatory email entry gate component"
      min_lines: 60
    - path: "frontend/src/pages/MarketplacePage.tsx"
      provides: "MarketplacePage with entry gate integration, old gate removed"
  key_links:
    - from: "frontend/src/components/marketplace/EmailEntryGate.tsx"
      to: "frontend/src/store/nltrStore.ts"
      via: "useNltrStore.setSubscribed(email)"
      pattern: "setSubscribed"
    - from: "frontend/src/pages/MarketplacePage.tsx"
      to: "frontend/src/components/marketplace/EmailEntryGate.tsx"
      via: "Conditional render when !subscribed"
      pattern: "EmailEntryGate"
    - from: "frontend/src/pages/MarketplacePage.tsx"
      to: "/api/newsletter/subscribe"
      via: "fetch with source: page_entry"
      pattern: "page_entry"
---

<objective>
Create the mandatory email entry gate that blocks the Explorer until email submission, replace the old newsletter gate, and send the correct Loops source tag.

Purpose: Capture every visitor's email upfront before they browse experts — the core conversion gate for lead generation.
Output: EmailEntryGate component + MarketplacePage integration with old gate removed.
</objective>

<execution_context>
@/Users/sebastianhamers/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sebastianhamers/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/64-email-first-gate/64-RESEARCH.md
@.planning/phases/64-email-first-gate/64-CONTEXT.md
@frontend/src/pages/MarketplacePage.tsx
@frontend/src/store/nltrStore.ts
@frontend/src/tracking.ts
@frontend/src/components/marketplace/NewsletterGateModal.tsx
@app/routers/newsletter.py
@app/loops.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create EmailEntryGate component</name>
  <files>frontend/src/components/marketplace/EmailEntryGate.tsx</files>
  <action>
Create a new `EmailEntryGate.tsx` component — a full-screen mandatory email gate overlay.

**Layout (from CONTEXT.md):**
- Full-screen overlay (`fixed inset-0 z-50`) with blurred backdrop (`backdrop-blur-md bg-black/50`)
- Centered card using glassmorphic styling matching existing NewsletterGateModal (`bg-white/10 backdrop-blur-xl border border-white/20 rounded-2xl`)
- Card max-width: `max-w-md w-full mx-4`

**Card content (top to bottom):**
1. Tinrate logo at top — use text-based logo: bold "Tinrate" in white, or a small image if one exists in the repo (check `frontend/public/` or `frontend/src/assets/`). If no logo image, use stylized text.
2. Tagline below logo — a concise value proposition. Tone: blend of value-first and professional. Do NOT mention number of experts. Example: "Discover top consultants matched to your exact needs" or similar. Claude chooses exact wording.
3. Email input field — styled like existing gates (`bg-white/10 border border-white/20 rounded-xl px-4 py-3 text-white placeholder-white/40`)
4. Submit button — text "Get Access", styled `bg-white text-gray-900 hover:bg-white/90 rounded-xl font-semibold py-3 w-full`
5. Privacy note below button — small text, e.g. "We respect your privacy. No spam." (`text-white/40 text-xs text-center mt-2`)
6. Inline error message for invalid email — shown between input and button when validation fails (`text-red-400 text-xs`)

**Behavior:**
- Validate on submit only (not real-time). Use regex: `/^[^\s@]+@[^\s@]+\.[^\s@]+$/`
- NO close button, NO dismiss handler, NO overlay click handler, NO Escape key handler
- On valid submit: call `onSubmit(email)` prop
- Component wrapped in `motion.div` from `motion/react` with:
  - initial={{ opacity: 0 }}, animate={{ opacity: 1 }} for entry
  - exit={{ opacity: 0 }} with transition={{ duration: 0.4, ease: 'easeOut' }} for smooth fade-out
- Inner card also animated: initial={{ scale: 0.95, opacity: 0 }}, animate={{ scale: 1, opacity: 1 }}

**Props interface:**
```typescript
interface EmailEntryGateProps {
  onSubmit: (email: string) => void
}
```

**Important:** This component does NOT manage subscribed state — the parent (MarketplacePage) controls visibility via AnimatePresence.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Verify component file exists and exports EmailEntryGate</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>EmailEntryGate.tsx exists with full-screen overlay, glassmorphic card, email form, "Get Access" button, privacy note, validation-on-submit, no dismiss paths, motion animations</done>
</task>

<task type="auto">
  <name>Task 2: Integrate entry gate into MarketplacePage and remove old newsletter gate</name>
  <files>frontend/src/pages/MarketplacePage.tsx, frontend/src/components/marketplace/NewsletterGateModal.tsx</files>
  <action>
Modify `MarketplacePage.tsx` to:

**1. Replace the old newsletter gate with the new entry gate:**
- Remove `import { NewsletterGateModal }` and the `<NewsletterGateModal>` render at bottom
- Remove the old gate state: `showGate`, `pendingProfileUrl`, `setPendingProfileUrl`
- Remove `handleDismiss` function entirely
- Remove the profile-gate logic from `handleViewProfile` — it should now ALWAYS open the profile since the entry gate already captured email
- Simplify `handleViewProfile` to just: `window.open(url, '_blank', 'noopener,noreferrer')`

**2. Add the new entry gate:**
- Import `EmailEntryGate` from `../components/marketplace/EmailEntryGate`
- Import `AnimatePresence` from `motion/react`
- Read `subscribed` from `useNltrStore` (already imported)
- Render `<AnimatePresence>` wrapping the entry gate INSIDE the main layout div, AFTER the AuroraBackground opening tag and BEFORE the main content div:
```tsx
<AuroraBackground>
  <AnimatePresence>
    {!subscribed && <EmailEntryGate onSubmit={handleGateSubmit} />}
  </AnimatePresence>
  <div className="flex flex-col h-screen overflow-x-hidden">
    {/* existing content */}
  </div>
</AuroraBackground>
```

**3. Remove legacy localStorage bypass checks:**
- Remove the `legacyUnlocked` check that reads `tcs_gate_email` and `tcs_email_unlocked`
- Remove `isUnlocked` variable — it's replaced by `subscribed` from useNltrStore
- The entry gate now only checks `subscribed` from Zustand persist (synchronous, no flash per GATE-03)

**4. Create `handleGateSubmit` function:**
```typescript
function handleGateSubmit(email: string) {
  // 1. Write to Zustand store immediately — this dismisses the gate
  setSubscribed(email)

  // 2. Delayed subscribe call — gives time for first search query
  //    to fire with email before Loops sync
  setTimeout(() => {
    const sessionId = localStorage.getItem('tcs_session_id')
    fetch(`${API_URL}/api/newsletter/subscribe`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        email,
        source: 'page_entry',
        session_id: sessionId,
      }),
    }).catch(() => {})
  }, 3000)
}
```

Key points:
- `source: 'page_entry'` (not 'gate') per GATE-04
- 3-second delay on the subscribe call per CONTEXT.md decision: "Loops API call is delayed after gate submission"
- `setSubscribed(email)` called immediately — gate disappears instantly, user can start searching
- The old `handleSubscribe` function is removed entirely

**5. Delete the old NewsletterGateModal file** since it's no longer used anywhere:
- Delete or empty `frontend/src/components/marketplace/NewsletterGateModal.tsx`
- Actually: just remove the file content and replace with a comment noting it was replaced by EmailEntryGate. Or better: check if it's imported anywhere else first. If only imported in MarketplacePage, delete the file entirely.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Open Explorer in incognito — gate should appear. Submit email — gate fades, Explorer shows. Refresh — no gate. Check Network tab for /api/newsletter/subscribe call with source: page_entry after ~3s delay.</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>MarketplacePage renders EmailEntryGate for unsubscribed visitors, bypasses instantly for returning subscribers (no flash), sends source "page_entry" to subscribe endpoint with 3s delay, old NewsletterGateModal removed, legacy localStorage checks removed, handleViewProfile simplified</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` passes with no errors
- `cd frontend && npx vitest run` passes (existing tests still green)
- New visitor in incognito: sees full-screen blurred gate before any expert cards
- Email submission: gate fades out smoothly, Explorer loads
- Returning subscriber (localStorage has tinrate-newsletter-v1 with subscribed:true): no gate, no flash
- Click outside gate overlay: nothing happens (gate stays)
- Press Escape: nothing happens (gate stays)
- Network tab: POST /api/newsletter/subscribe fires ~3s after submission with body containing `source: "page_entry"`
</verification>

<success_criteria>
- GATE-01: New visitor sees mandatory gate before browsing
- GATE-02: Gate cannot be dismissed except by email submission
- GATE-03: Returning subscriber bypasses gate with no flash
- GATE-04: Loops receives source: "page_entry"
- Old NewsletterGateModal removed
- TypeScript compiles, existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/64-email-first-gate/64-01-SUMMARY.md`
</output>
