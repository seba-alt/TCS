---
phase: 67-email-gate-polish-list-view-fix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/marketplace/ExpertList.tsx
autonomous: true
requirements: [FIX-01]

must_haves:
  truths:
    - "Each expert row in list view displays a save/bookmark button"
    - "Clicking the bookmark toggles saved state via toggleSavedExpert"
    - "Bookmark icon shows filled state when saved, unfilled when not saved"
    - "Bookmark behavior (toggle, visual states) matches grid view ExpertCard exactly"
  artifacts:
    - path: "frontend/src/components/marketplace/ExpertList.tsx"
      provides: "List view rows with bookmark button matching grid view"
  key_links:
    - from: "frontend/src/components/marketplace/ExpertList.tsx"
      to: "frontend/src/store/filterSlice.ts"
      via: "toggleSavedExpert action from useFilterSlice"
      pattern: "toggleSavedExpert\\(expert\\.username\\)"
---

<objective>
Add save/bookmark button to list view expert rows, matching grid view behavior.

Purpose: Users can bookmark experts from list view just like grid view — fixing a parity gap where list view was missing this feature (FIX-01).
Output: Updated ExpertList.tsx with bookmark buttons on each row.
</objective>

<execution_context>
@/Users/sebastianhamers/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sebastianhamers/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/components/marketplace/ExpertList.tsx
@frontend/src/components/marketplace/ExpertCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add bookmark button to list view expert rows</name>
  <files>frontend/src/components/marketplace/ExpertList.tsx</files>
  <action>
    1. **Import Bookmark icon:** Add `import { Bookmark } from 'lucide-react'` at the top of ExpertList.tsx.

    2. **Access savedExperts and toggleSavedExpert:** The component already imports `useFilterSlice` and destructures `savedExperts`. Also destructure `toggleSavedExpert` from the same hook: `const { savedFilter, savedExperts, toggleSavedExpert } = useFilterSlice()`.

    3. **Add bookmark button to each row:** Inside the `itemContent` render function (the `div` with `className="flex items-center gap-4 px-4 py-3 ..."`), add a bookmark button. Position it in the row — recommended placement: after the rate span, before domain tags (or as the last element). Use a simple icon button.

       Pattern to match from ExpertCard.tsx:
       ```tsx
       const isSaved = savedExperts.includes(expert.username)

       <button
         onClick={(e) => { e.stopPropagation(); toggleSavedExpert(expert.username) }}
         className="p-1 text-gray-400 hover:text-brand-purple transition-colors shrink-0"
         aria-label={isSaved ? 'Remove bookmark' : 'Bookmark expert'}
       >
         <Bookmark size={16} className={isSaved ? 'fill-current text-brand-purple' : ''} />
       </button>
       ```

    4. **Placement:** Per CONTEXT.md decision: "positioned in the top-right corner of each list view expert card" and "Always visible (not hover-only)". For list view rows (which are horizontal, not card-like), "top-right corner" translates to the right side of the row. Place the bookmark button as the last element in the flex row (after domain tags), or between rate and tags. The button should use `shrink-0` to prevent compression.

    5. **Visual states:** Match ExpertCard exactly:
       - Unsaved: `text-gray-400` with `hover:text-brand-purple`
       - Saved: `fill-current text-brand-purple` on the Bookmark icon
       - Row background: Optionally add saved highlight like ExpertCard does (`bg-purple-50 border-purple-200` when saved). For list rows, a subtle `bg-purple-50/50` when saved would provide visual feedback without being heavy.

    6. **`e.stopPropagation()`:** Critical — the row div has an `onClick` for `onViewProfile`. The bookmark click must stop propagation so it doesn't trigger profile navigation.
  </action>
  <verify>
    <automated>cd /Users/sebastianhamers/Documents/TCS && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Switch to list view. Each expert row shows a bookmark icon on the right. Click it — icon fills with purple. Click again — unfills. Switching to grid view, same expert shows matching saved state. No profile navigation triggered when clicking bookmark.</manual>
  </verify>
  <done>Every list view expert row renders a visible bookmark button with filled/unfilled toggle states matching grid view. Clicking toggles saved state without triggering profile navigation.</done>
</task>

</tasks>

<verification>
- [ ] List view expert rows each have a visible bookmark button (FIX-01)
- [ ] Bookmark icon toggles between filled (saved) and unfilled (not saved) states
- [ ] Visual states (icon, color) match grid view ExpertCard bookmark exactly
- [ ] Clicking bookmark does NOT trigger profile navigation (stopPropagation)
- [ ] Saved state is shared between grid and list view (same Zustand store)
- [ ] TypeScript compiles without errors
</verification>

<success_criteria>
List view renders a save/bookmark button for each expert matching grid view behavior (FIX-01). Users can bookmark from either view mode with consistent state.
</success_criteria>

<output>
After completion, create `.planning/phases/67-email-gate-polish-list-view-fix/67-02-SUMMARY.md`
</output>
