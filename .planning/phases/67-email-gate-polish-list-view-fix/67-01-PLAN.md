---
phase: 67-email-gate-polish-list-view-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/marketplace/EmailEntryGate.tsx
  - frontend/src/pages/MarketplacePage.tsx
  - frontend/src/components/Header.tsx
  - frontend/public/logo-dark-bg.png
autonomous: true
requirements: [GATE-01, GATE-02, GATE-03]

must_haves:
  truths:
    - "Email gate displays dark charcoal (#1a1a2e) semi-transparent overlay with the dark-bg logo and minimal copy"
    - "Email input auto-focuses when gate appears"
    - "Search bar behind overlay is not accessible (overlay blocks interaction)"
    - "Both Enter key and button click submit the email form"
    - "Gate dismisses with a smooth fade-out transition (~300ms)"
    - "After gate dismissal, search bar receives focus automatically"
  artifacts:
    - path: "frontend/public/logo-dark-bg.png"
      provides: "Dark-background logo image for the gate"
    - path: "frontend/src/components/marketplace/EmailEntryGate.tsx"
      provides: "Redesigned gate component with dark overlay, minimal text, auto-focus"
    - path: "frontend/src/pages/MarketplacePage.tsx"
      provides: "Post-gate search bar focus callback"
    - path: "frontend/src/components/Header.tsx"
      provides: "Exposed focusSearchBar method via imperative handle or callback"
  key_links:
    - from: "frontend/src/pages/MarketplacePage.tsx"
      to: "frontend/src/components/Header.tsx"
      via: "ref or callback to focus search bar after gate dismissal"
      pattern: "focusSearchBar|inputRef\\.current\\.focus"
    - from: "frontend/src/components/marketplace/EmailEntryGate.tsx"
      to: "frontend/src/pages/MarketplacePage.tsx"
      via: "onSubmit callback triggers gate dismiss + search focus"
      pattern: "onSubmit.*handleGateSubmit"
---

<objective>
Redesign the email entry gate for a cleaner, more focused experience and implement auto-focus behaviors.

Purpose: Users see a polished dark-themed gate with minimal text, the input auto-focuses, and after submission the search bar gets focus — creating a smooth onboarding flow (GATE-01, GATE-02, GATE-03).
Output: Redesigned EmailEntryGate.tsx, updated MarketplacePage.tsx with post-dismiss focus, Header.tsx with focus API.
</objective>

<execution_context>
@/Users/sebastianhamers/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sebastianhamers/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/components/marketplace/EmailEntryGate.tsx
@frontend/src/pages/MarketplacePage.tsx
@frontend/src/components/Header.tsx
@frontend/src/store/nltrStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move dark-bg logo and redesign EmailEntryGate with dark overlay and minimal copy</name>
  <files>
    frontend/public/logo-dark-bg.png
    frontend/src/components/marketplace/EmailEntryGate.tsx
  </files>
  <action>
    1. **Move the dark-bg logo:** Copy `frontend/For Dark bg.png` to `frontend/public/logo-dark-bg.png` (spaces in filename are problematic for web serving — rename on copy).

    2. **Redesign EmailEntryGate.tsx** with these locked decisions from CONTEXT.md:
       - **Background:** Dark charcoal (#1a1a2e) as a semi-transparent overlay. Replace the current `backdrop-blur-md bg-black/50` with something like `bg-[#1a1a2e]/95 backdrop-blur-sm`. The page content should be dimmed but barely visible behind it.
       - **Logo:** Replace `/logo.png` with `/logo-dark-bg.png` (the "For Dark bg.png" image). Keep reasonable sizing (h-8 to h-10).
       - **Copy:** Minimal — short heading like "Get Access" and one brief line like "Enter your email to unlock." Remove the current lengthy tagline ("Find the Right Expert, Instantly" + "Browse verified consultants matched to your exact needs. Enter your email to get started."). Keep it to one heading + one short line max.
       - **Input:** Keep the existing email input with `autoFocus` attribute (already present). Ensure it works with both Enter key (form submit) and button click (already works via form onSubmit).
       - **Button text:** "Get Access" (matches CONTEXT.md tone).
       - **Privacy note:** Keep the existing brief "We respect your privacy. No spam." or similar minimal footer.
       - **Overall feel:** Clean, dark, minimal — logo + short text + input + button. No glassmorphic card — the overlay itself IS the container.

    3. **Gate dismiss animation:** The existing motion/react exit animation handles fade-out via AnimatePresence in MarketplacePage.tsx. Adjust the exit transition duration to ~300ms (currently 0.4s/0.3s). Set `exit={{ opacity: 0 }}` transition to `{ duration: 0.3 }` for the outer overlay.

    4. **Auto-focus:** The input already has `autoFocus`. This satisfies GATE-02 for the input side. The overlay itself blocks the search bar behind it (z-50 fixed overlay), which satisfies the "search bar cannot be interacted with" requirement naturally.
  </action>
  <verify>
    <automated>cd /Users/sebastianhamers/Documents/TCS && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Open the app with cleared localStorage (no tinrate-newsletter-v1 key). Verify: dark overlay appears, dark-bg logo visible, minimal copy, input auto-focused, can submit via Enter or button click, gate fades out smoothly.</manual>
  </verify>
  <done>Email gate shows dark charcoal overlay with dark-bg logo, minimal "Get Access" copy, auto-focused input, and ~300ms fade-out exit animation. Search bar behind overlay is blocked by z-50 overlay.</done>
</task>

<task type="auto">
  <name>Task 2: Wire post-gate-dismiss search bar focus (GATE-03)</name>
  <files>
    frontend/src/components/Header.tsx
    frontend/src/pages/MarketplacePage.tsx
  </files>
  <action>
    1. **Header.tsx — expose a focus method:**
       - Use `useImperativeHandle` with `forwardRef` to expose a `focusSearchBar()` method from Header.
       - OR simpler: accept an optional `searchInputRef` prop (React.RefObject&lt;HTMLInputElement&gt;) and assign it to the search input alongside the existing local inputRef (use a callback ref to assign both).
       - **Recommended approach:** Use `forwardRef` + `useImperativeHandle` to expose `{ focusSearchBar: () => void }`. Inside, call `inputRef.current?.focus()`.
       - Update the Header export: `export default` stays the same but wrap with `forwardRef`.

    2. **MarketplacePage.tsx — call focus after gate submit:**
       - Create a ref for Header: `const headerRef = useRef<{ focusSearchBar: () => void }>(null)`
       - Pass ref to Header: `<Header ref={headerRef} />`
       - In `handleGateSubmit`, after `setSubscribed(email)`, add a short delay (e.g., `setTimeout(() => headerRef.current?.focusSearchBar(), 350)`) — the 350ms allows the gate fade-out animation (~300ms) to complete before focusing the search bar.
       - Keep the existing delayed subscribe call logic unchanged.

    3. **Remove the Header auto-focus on mount:** The current `useEffect(() => { inputRef.current?.focus() }, [])` in Header.tsx auto-focuses the search bar on mount. This conflicts with the gate — when the gate is showing, the search bar should NOT be focused (the gate input should be). Remove this auto-focus useEffect. The search bar will get focus via the explicit `focusSearchBar()` call after gate dismissal. For returning users (gate already dismissed), the search bar will also receive focus via the same mechanism — OR conditionally auto-focus only when already subscribed. Check `useNltrStore().subscribed`: if true, auto-focus on mount; if false, skip (gate will handle focus flow).
  </action>
  <verify>
    <automated>cd /Users/sebastianhamers/Documents/TCS && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Clear localStorage, reload. Gate appears with focused email input. Submit email. Gate fades out. Search bar receives focus automatically. For returning users (subscribed=true), search bar auto-focuses on page load as before.</manual>
  </verify>
  <done>After gate submission and fade-out, search bar auto-focuses. Returning users still get auto-focused search bar on load. Gate input focus and search bar focus are properly sequenced.</done>
</task>

</tasks>

<verification>
- [ ] Gate displays dark charcoal (#1a1a2e) overlay with dark-bg logo (GATE-01)
- [ ] Gate shows minimal copy — short heading + one line, no verbose descriptions (GATE-01)
- [ ] Email input auto-focuses when gate appears (GATE-02)
- [ ] Search bar behind overlay is not accessible (GATE-02)
- [ ] Gate dismisses with ~300ms fade-out (GATE-01)
- [ ] After gate dismissal, search bar receives focus (GATE-03)
- [ ] Returning users (already subscribed) get search bar auto-focus on load
- [ ] TypeScript compiles without errors
</verification>

<success_criteria>
All three GATE requirements are met: the email gate has a dark overlay with dark-bg logo and minimal text (GATE-01), the email input auto-focuses and the search bar is blocked (GATE-02), and after dismissal the search bar auto-focuses (GATE-03).
</success_criteria>

<output>
After completion, create `.planning/phases/67-email-gate-polish-list-view-fix/67-01-SUMMARY.md`
</output>
