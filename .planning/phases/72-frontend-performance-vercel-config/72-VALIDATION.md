---
phase: 72
slug: frontend-performance-vercel-config
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-05
---

# Phase 72 — Validation Strategy

> Generated by `gsd-phase-researcher` during `/gsd:plan-phase 72`.
> Updated by `gsd-plan-checker` after plan approval.
> Governs feedback sampling during `/gsd:execute-phase 72`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest 4.0.18 |
| **Config file** | `frontend/vite.config.ts` (test section) |
| **Quick run command** | `cd frontend && npx vitest run src/tracking.test.ts` |
| **Full suite command** | `cd frontend && npm test` |
| **Estimated runtime** | ~5 seconds |
| **CI pipeline** | None — manual verification |

---

## Nyquist Sampling Rate

> The minimum feedback frequency required to reliably catch errors in this phase.

- **After every task commit:** Run `cd frontend && npx vitest run src/tracking.test.ts`
- **After every plan wave:** Run `cd frontend && npm test && npm run build`
- **Before `/gsd:verify-work`:** Full suite must be green + build produces expected chunks
- **Maximum acceptable task feedback latency:** 8 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 72-01-01 | 01 | 1 | FPERF-01 | unit | `cd frontend && npx vitest run src/tracking.test.ts` | Yes (needs batch tests) | pending |
| 72-01-02 | 01 | 1 | FPERF-01 | unit (backend) | `grep "events/batch" app/routers/events.py` | No (new endpoint) | pending |
| 72-01-03 | 01 | 1 | FPERF-02 | build | `cd frontend && npm run build 2>&1 \| grep vendor` | N/A (build check) | pending |
| 72-01-04 | 01 | 1 | FPERF-03 | static | `grep preconnect frontend/index.html` | N/A (grep check) | pending |
| 72-02-01 | 02 | 1 | VCFG-01 | config | `grep immutable frontend/vercel.json` | N/A (config check) | pending |
| 72-02-02 | 02 | 1 | VCFG-02 | config | `grep stale-while-revalidate frontend/vercel.json` | N/A (config check) | pending |

*Status values: pending / green / red / flaky*

---

## Wave 0 Requirements

> Test scaffolding committed BEFORE any implementation task. Executor runs Wave 0 first.

Existing infrastructure covers all phase requirements — no Wave 0 test tasks needed. `tracking.test.ts` already exists; new batch-specific tests are part of FPERF-01 implementation. FPERF-02/03 and VCFG-01/02 are config-only verified by build/grep.

---

## Manual-Only Verifications

> Behaviors that genuinely cannot be automated, with justification.
> These are surfaced during `/gsd:verify-work` UAT.

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Network tab shows single batch POST | FPERF-01 | Requires browser DevTools inspection | Open site, trigger events, check Network tab for single POST to /api/events/batch |
| `npm run preview` shows zero console errors | FPERF-02 | Requires manual browser inspection after build | Run `npm run build && npm run preview`, open browser, check console |

---

## Validation Sign-Off

Updated by `gsd-plan-checker` when plans are approved:

- [ ] All tasks have automated verify commands or Wave 0 dependencies
- [ ] No 3 consecutive implementation tasks without automated verify (sampling continuity)
- [ ] Wave 0 test files cover all MISSING references
- [ ] No watch-mode flags in any automated command
- [ ] Feedback latency per task: < 8s
- [ ] `nyquist_compliant: true` set in frontmatter

**Plan-checker approval:** pending

---

## Execution Tracking

Updated during `/gsd:execute-phase 72`:

| Wave | Tasks | Tests Run | Pass | Fail | Sampling Status |
|------|-------|-----------|------|------|-----------------|
| 1 | TBD | TBD | TBD | TBD | pending |

**Phase validation complete:** pending
