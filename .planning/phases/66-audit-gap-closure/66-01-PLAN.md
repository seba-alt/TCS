---
phase: 66-audit-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/routers/admin/leads.py
  - .planning/phases/64-email-first-gate/64-VERIFICATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [GATE-01, GATE-02, GATE-03, GATE-04, TRACK-03]
gap_closure: true

must_haves:
  truths:
    - "explorer_click entries in admin lead timeline show the correct expert username (not blank)"
    - "Phase 64 VERIFICATION.md exists and confirms GATE-01 through GATE-04 and TRACK-03 are satisfied"
    - "REQUIREMENTS.md marks GATE-01, GATE-02, GATE-03, GATE-04, and TRACK-03 as done"
  artifacts:
    - path: "app/routers/admin/leads.py"
      provides: "Fixed explorer_click payload key extraction"
      contains: "payload_data.get(\"expert_id\""
    - path: ".planning/phases/64-email-first-gate/64-VERIFICATION.md"
      provides: "Formal Phase 64 verification against success criteria"
      contains: "status: passed"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated requirement checkboxes for GATE-01-04 and TRACK-03"
      contains: "[x] **GATE-01**"
  key_links:
    - from: "app/routers/admin/leads.py"
      to: "frontend/src/components/marketplace/ExpertCard.tsx"
      via: "payload key agreement on expert_id"
      pattern: "payload_data\\.get\\(\"expert_id\""
---

<objective>
Close all gaps identified by the v5.2 milestone audit: fix the explorer_click payload key bug in leads.py and create the missing Phase 64 VERIFICATION.md.

Purpose: The milestone audit found 5 partial requirements (GATE-01 through GATE-04 and TRACK-03) due to a missing verification document and one payload key mismatch bug. This plan fixes both issues to bring v5.2 to full satisfaction.

Output: Fixed leads.py, Phase 64 VERIFICATION.md, updated REQUIREMENTS.md
</objective>

<execution_context>
@/Users/sebastianhamers/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sebastianhamers/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v5.2-MILESTONE-AUDIT.md
@.planning/phases/66-audit-gap-closure/66-RESEARCH.md
@.planning/phases/64-email-first-gate/64-01-SUMMARY.md
@.planning/phases/64-email-first-gate/64-02-SUMMARY.md
@.planning/phases/65-admin-enhancements/65-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix explorer_click payload key mismatch in leads.py</name>
  <files>app/routers/admin/leads.py</files>
  <action>
    In `app/routers/admin/leads.py`, find lines 162-163 inside the `elif row.event_type == "card_click":` block.

    Change BOTH occurrences of `payload_data.get("expert", "")` to `payload_data.get("expert_id", "")`.

    Before (lines 162-163):
    ```python
    "expert_username": payload_data.get("expert", ""),
    "expert_name": payload_data.get("expert", ""),
    ```

    After:
    ```python
    "expert_username": payload_data.get("expert_id", ""),
    "expert_name": payload_data.get("expert_id", ""),
    ```

    This aligns the backend with the frontend `ExpertCard.tsx` which sends `expert_id: expert.username` in the trackEvent payload. Both `expert_username` and `expert_name` resolve to the username string — this is correct and consistent with the audit fix specification. Do NOT add a DB lookup for display name resolution (out of scope).

    IMPORTANT: Fix BOTH lines, not just one. They are adjacent and identical.
  </action>
  <verify>
    <automated>cd /Users/sebastianhamers/Documents/TCS && python3 -c "import ast; ast.parse(open('app/routers/admin/leads.py').read()); print('Python syntax OK')" && grep -n 'expert_id' app/routers/admin/leads.py | head -5</automated>
    <manual>Verify lines 162-163 both read expert_id, not expert</manual>
  </verify>
  <done>Both expert_username and expert_name fields in the explorer_click event dict read from payload key "expert_id" instead of "expert". Python file parses without syntax errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create Phase 64 VERIFICATION.md and update REQUIREMENTS.md</name>
  <files>
    .planning/phases/64-email-first-gate/64-VERIFICATION.md
    .planning/REQUIREMENTS.md
  </files>
  <action>
    **Part A — Create Phase 64 VERIFICATION.md:**

    Create `.planning/phases/64-email-first-gate/64-VERIFICATION.md` following the exact format of Phase 65's `65-VERIFICATION.md`. Use this structure:

    Frontmatter:
    ```yaml
    ---
    phase: 64
    name: Email-First Gate
    status: passed
    verified_at: 2026-03-04T00:00:00Z
    verifier: orchestrator-inline
    ---
    ```

    Content sections (follow 65-VERIFICATION.md pattern exactly):
    1. **Goal** — from ROADMAP.md Phase 64 goal: "Every new visitor sees the email gate before browsing the Explorer, returning subscribers bypass it instantly, and lead timelines show post-gate search activity attributed by email"
    2. **Requirements Verification** table — 5 requirements (GATE-01 through GATE-04, TRACK-03), all PASSED, with code evidence:
       - GATE-01: `EmailEntryGate.tsx` renders full-screen gate; `MarketplacePage.tsx` conditionally renders on `!subscribed`
       - GATE-02: `EmailEntryGate.tsx` has no dismiss path — no onClose, no backdrop click handler, no Escape key handler
       - GATE-03: Synchronous Zustand persist initializer via `useState` lazy init in `MarketplacePage.tsx` — no flash because check runs before first render
       - GATE-04: `MarketplacePage.tsx` sends `source: "page_entry"` in delayed subscribe call (3s setTimeout)
       - TRACK-03: `leads.py` step 3.5 queries `user_events WHERE email = :email`; `explorer_search` and `explorer_click` events rendered in LeadsPage.tsx. Note: `explorer_click` payload key bug corrected in Phase 66.
    3. **Success Criteria Verification** table — 6 criteria from ROADMAP.md Phase 64, all PASSED with evidence
    4. **Must-Have Artifact Checks** table — key files and their expected content
    5. **TypeScript Compilation** — reference 64-01-SUMMARY.md self-check (tsc --noEmit passed)
    6. **Automated Checks Summary** — counts
    7. **Result: PASSED**

    For TRACK-03, mark as PASSED with this note: "Note: `explorer_click` payload key bug (`expert` vs `expert_id`) discovered post-hoc and corrected in Phase 66."

    **Part B — Update REQUIREMENTS.md:**

    In `.planning/REQUIREMENTS.md`, change the 5 unchecked requirements to checked:
    - `- [ ] **GATE-01**` → `- [x] **GATE-01**`
    - `- [ ] **GATE-02**` → `- [x] **GATE-02**`
    - `- [ ] **GATE-03**` → `- [x] **GATE-03**`
    - `- [ ] **GATE-04**` → `- [x] **GATE-04**`
    - `- [ ] **TRACK-03**` → `- [x] **TRACK-03**`

    Also update the Traceability table: change status from "Pending" to "Done" for GATE-01, GATE-02, GATE-03, GATE-04, and TRACK-03.

    Update the Coverage section: change "Pending (gap closure): 5" to "Pending (gap closure): 0" and confirm "Unmapped: 0".
  </action>
  <verify>
    <automated>cd /Users/sebastianhamers/Documents/TCS && test -f .planning/phases/64-email-first-gate/64-VERIFICATION.md && grep -c "PASSED" .planning/phases/64-email-first-gate/64-VERIFICATION.md && grep -c "\[x\] \*\*GATE" .planning/REQUIREMENTS.md && grep -c "\[x\] \*\*TRACK-03" .planning/REQUIREMENTS.md</automated>
    <manual>Verify VERIFICATION.md has correct frontmatter, all 5 requirements listed as PASSED, and REQUIREMENTS.md has all checkboxes checked</manual>
  </verify>
  <done>Phase 64 VERIFICATION.md exists with status: passed, all 5 requirements marked PASSED with code evidence, TRACK-03 includes Phase 66 fix note. REQUIREMENTS.md has all 11 v5.2 requirements checked as done with traceability table updated.</done>
</task>

</tasks>

<verification>
1. `python3 -c "import ast; ast.parse(open('app/routers/admin/leads.py').read())"` — Python syntax valid
2. `grep 'expert_id' app/routers/admin/leads.py` — shows both lines 162-163 use expert_id
3. `grep -c 'payload_data.get("expert",` app/routers/admin/leads.py` — returns 0 (old key gone)
4. `test -f .planning/phases/64-email-first-gate/64-VERIFICATION.md` — file exists
5. `grep 'status: passed' .planning/phases/64-email-first-gate/64-VERIFICATION.md` — verification passed
6. `grep -c '\[ \]' .planning/REQUIREMENTS.md` — returns 0 (no unchecked v5.2 requirements)
7. `cd frontend && npx vitest run --reporter=verbose` — all 17 tests pass (regression guard)
</verification>

<success_criteria>
- explorer_click payload reads expert_id key (not expert) in leads.py
- Phase 64 VERIFICATION.md exists with PASSED status and evidence for all 5 requirements
- REQUIREMENTS.md shows all 11 v5.2 requirements as done
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/66-audit-gap-closure/66-01-SUMMARY.md`
</output>
