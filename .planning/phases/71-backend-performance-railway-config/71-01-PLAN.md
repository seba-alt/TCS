---
phase: 71-backend-performance-railway-config
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/routers/health.py
  - app/routers/admin/experts.py
  - frontend/src/admin/hooks/useAdminData.ts
  - frontend/src/admin/pages/ExpertsPage.tsx
  - frontend/src/admin/pages/OverviewPage.tsx
autonomous: true
requirements:
  - BPERF-01
  - BPERF-02
  - BPERF-03

must_haves:
  truths:
    - "GET /api/health returns {status, index_size} fast and unauthenticated for Railway healthcheck"
    - "GET /api/admin/health returns DB status, expert count, db_latency_ms, faiss_vectors, uptime_s, version — requires admin JWT"
    - "GET /api/admin/experts?page=0&limit=50 returns paginated experts with total, page, total_pages fields"
    - "GET /api/admin/experts?search=john filters experts by name"
    - "Admin overview page shows richer health data from the authenticated health endpoint"
    - "Photo proxy already returns Cache-Control: public, max-age=86400 (BPERF-03 is verification-only)"
  artifacts:
    - path: "app/routers/health.py"
      provides: "Two-tier health endpoints"
      contains: "/api/admin/health"
    - path: "app/routers/admin/experts.py"
      provides: "Paginated experts endpoint with search"
      contains: "total_pages"
    - path: "frontend/src/admin/pages/ExpertsPage.tsx"
      provides: "Server-side paginated experts table with AdminPagination"
      contains: "AdminPagination"
    - path: "frontend/src/admin/pages/OverviewPage.tsx"
      provides: "Admin health widget with DB diagnostics"
      contains: "/api/admin/health"
  key_links:
    - from: "app/routers/health.py"
      to: "app/routers/admin/_common.py"
      via: "import _require_admin"
      pattern: "from app\\.routers\\.admin\\._common import.*_require_admin"
    - from: "frontend/src/admin/pages/ExpertsPage.tsx"
      to: "/api/admin/experts?page=&limit=&search="
      via: "adminFetch with pagination params"
      pattern: "page=.*limit=.*search="
    - from: "frontend/src/admin/pages/OverviewPage.tsx"
      to: "/api/admin/health"
      via: "adminFetch for rich health data"
      pattern: "/api/admin/health"
---

<objective>
Enhance the health endpoint with a two-tier design and add server-side pagination to the admin experts list.

Purpose: Railway needs a fast unauthenticated healthcheck while the admin panel needs rich diagnostics. The experts endpoint currently returns all 530+ experts in one response, triggering Sentry large-payload alerts. Server-side pagination fixes this. BPERF-03 (photo proxy Cache-Control) is already implemented — this plan verifies it.

Output: Two health endpoints (public + admin), paginated admin experts API + frontend wiring, upgraded admin health widget.
</objective>

<execution_context>
@/Users/sebastianhamers/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sebastianhamers/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-backend-performance-railway-config/71-RESEARCH.md

@app/routers/health.py
@app/routers/admin/experts.py
@app/routers/admin/_common.py
@app/database.py
@frontend/src/admin/hooks/useAdminData.ts
@frontend/src/admin/pages/ExpertsPage.tsx
@frontend/src/admin/pages/OverviewPage.tsx
@frontend/src/admin/components/AdminPagination.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Two-tier health endpoints + admin experts pagination backend</name>
  <files>app/routers/health.py, app/routers/admin/experts.py</files>
  <action>
**health.py — Two-tier design:**

1. Keep the existing `GET /api/health` endpoint as-is (returns `{status, index_size}` — fast, unauthenticated, for Railway healthcheck).

2. Add a new `GET /api/admin/health` endpoint that requires admin JWT authentication:
   - Import `_require_admin` from `app.routers.admin._common` (safe — no circular import exists)
   - Record `_start_time = time.time()` at module level for uptime calculation
   - Measure DB latency: execute `SELECT COUNT(*) FROM experts` inside a `SessionLocal()` context, time with `time.monotonic()`
   - Return: `{status, db, expert_count, db_latency_ms, faiss_vectors, uptime_s, version}` where:
     - `db` = "ok" or "error: {message}" (wrap in try/except)
     - `db_latency_ms` = round to 1 decimal
     - `faiss_vectors` = `request.app.state.faiss_index.ntotal`
     - `uptime_s` = `round(time.time() - _start_time)`
     - `version` = "v5.4"
   - Use `Depends(_require_admin)` for auth (same pattern as all admin endpoints)

**admin/experts.py — Server-side pagination:**

1. Update `get_experts()` to add Query params:
   - `page: int = Query(default=0, ge=0)` — 0-indexed page number
   - `limit: int = Query(default=50, ge=1, le=200)` — items per page (fixed at 50 per user decision, but allow up to 200 for API flexibility)
   - `search: str = Query(default="")` — text search filter

2. Add search filter: if `search` is non-empty, add a `.where()` clause filtering on `func.lower(Expert.first_name + " " + Expert.last_name).contains(search.lower())`
   - Import `func` from `sqlalchemy` (add to existing imports)

3. Change default sort to `Expert.first_name.asc(), Expert.last_name.asc()` (alphabetical A-Z per user decision, replacing the current findability_score sort)

4. Count total before pagination: `total = db.scalar(select(func.count()).select_from(stmt.subquery()))`

5. Apply pagination: `stmt.offset(page * limit).limit(limit)`

6. Return `{experts: [...], total, page, total_pages}` where `total_pages = math.ceil(total / limit) if total else 0`
   - Import `math` at the top

Note: The existing `_serialize_expert` helper is already imported and works correctly.
  </action>
  <verify>
    <automated>cd /Users/sebastianhamers/Documents/TCS && python -c "
from app.routers.health import router
routes = [r.path for r in router.routes]
assert '/api/health' in routes, f'Missing /api/health in {routes}'
assert '/api/admin/health' in routes, f'Missing /api/admin/health in {routes}'
print('Health routes OK:', routes)
"</automated>
    <manual>Verify the health endpoint returns expected fields and the experts endpoint accepts page/limit/search params</manual>
  </verify>
  <done>
    - GET /api/health returns {status: "ok", index_size: N} (unchanged, fast, unauthenticated)
    - GET /api/admin/health returns {status, db, expert_count, db_latency_ms, faiss_vectors, uptime_s, version} (requires admin JWT)
    - GET /api/admin/experts?page=0&limit=50&search=john returns {experts: [...], total, page, total_pages}
    - Default sort is first-name A-Z
    - Photo proxy Cache-Control header is already in place (BPERF-03 — verified by reading browse.py:196)
  </done>
</task>

<task type="auto">
  <name>Task 2: Admin frontend — paginated experts table + health widget upgrade</name>
  <files>frontend/src/admin/hooks/useAdminData.ts, frontend/src/admin/pages/ExpertsPage.tsx, frontend/src/admin/pages/OverviewPage.tsx</files>
  <action>
**useAdminData.ts — Add paginated experts hook:**

1. Create a new `useAdminExpertsPaginated(page: number, search: string)` hook (keep the existing `useAdminExperts` hook unchanged — it is used by SettingsPage and TagManagerPage which still need the full list):
   - Accept `page` (0-indexed) and `search` (string) as parameters
   - Fetch from `/experts?page=${page}&limit=50&search=${encodeURIComponent(search)}`
   - Use `adminFetch` (already exists) for authenticated GET
   - Return `{ data, loading, error, refetch }` where `data` has shape `{ experts: ExpertRow[], total: number, page: number, total_pages: number }`
   - Re-fetch when `page` or `search` changes (include in useEffect/useCallback dependency array)

**ExpertsPage.tsx — Wire server-side pagination:**

1. Replace `useAdminExperts()` with `useAdminExpertsPaginated(page, search)`:
   - Add `const [page, setPage] = useState(0)` state
   - Add `const [search, setSearch] = useState('')` state (debounced — use a 300ms timeout ref pattern)
   - Wire the search input that already exists on the page (AdminInput) to the server-side search param
   - When search changes, reset page to 0

2. Remove the client-side `useMemo` filtering/pagination logic (the server now handles this)

3. Wire `AdminPagination` component (already imported on line 8):
   - Pass `page={data?.page ?? 0}`, `totalPages={data?.total_pages ?? 0}`, `onPageChange={setPage}`
   - Place below the experts table

4. Show total count somewhere visible: e.g., "Showing {experts.length} of {total} experts"

5. The sort headers can remain as visual indicators but client-side sort should be removed or disabled since the server now returns a fixed A-Z sort. Simplest: remove the sort click handler and keep the headers as static labels.

6. After mutations (add, delete, classify, tag-all, ingest), call `refetch()` from the paginated hook to reload the current page.

**OverviewPage.tsx — Upgrade health widget:**

1. Replace the `useHealthCheck` hook to call the authenticated `/api/admin/health` endpoint instead of the public `/api/health`:
   - Use `adminFetch('/health')` (which calls the admin-prefixed path with JWT) — or explicitly call `adminFetch` with the full path `/api/admin/health` depending on how `adminFetch` constructs URLs
   - Check how `adminFetch` works in the codebase and use the correct path format
   - Fallback: if the admin health call fails (e.g., token expired), fall back to the public endpoint

2. Show additional health data in the status indicator: DB status, expert count, and DB latency — in addition to the existing up/down and round-trip latency
  </action>
  <verify>
    <automated>cd /Users/sebastianhamers/Documents/TCS/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Load admin experts page — verify pagination controls appear, search filters server-side, page navigation works</manual>
  </verify>
  <done>
    - ExpertsPage shows paginated experts (50 per page) with AdminPagination controls
    - Search input filters experts server-side (debounced)
    - Page resets to 0 when search changes
    - OverviewPage health widget shows DB status, expert count, DB latency from /api/admin/health
    - SettingsPage and TagManagerPage continue to work with the existing useAdminExperts hook (unchanged)
  </done>
</task>

</tasks>

<verification>
1. Backend: `GET /api/health` returns `{status: "ok", index_size: N}` — fast, unauthenticated
2. Backend: `GET /api/admin/health` with admin JWT returns `{status, db, expert_count, db_latency_ms, faiss_vectors, uptime_s, version}`
3. Backend: `GET /api/admin/experts?page=0&limit=50` returns paginated response with `total`, `page`, `total_pages`
4. Backend: `GET /api/admin/experts?search=john` filters by name
5. Frontend: ExpertsPage shows pagination controls and server-side search
6. Frontend: OverviewPage health widget shows enriched diagnostics
7. BPERF-03: Photo proxy at `browse.py:196` already returns `Cache-Control: public, max-age=86400` (no change needed — verification only)
</verification>

<success_criteria>
- Two health endpoints responding correctly (public + admin)
- Admin experts endpoint returns max 50 experts per page with pagination metadata
- Frontend experts page uses AdminPagination component with server-side page/search
- No TypeScript compilation errors in frontend
</success_criteria>

<output>
After completion, create `.planning/phases/71-backend-performance-railway-config/71-01-SUMMARY.md`
</output>
