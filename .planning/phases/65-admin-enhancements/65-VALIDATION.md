---
phase: 65
slug: admin-enhancements
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-04
---

# Phase 65 — Validation Strategy

> Generated by `gsd-phase-researcher` during `/gsd:plan-phase 65`.
> Updated by `gsd-plan-checker` after plan approval.
> Governs feedback sampling during `/gsd:execute-phase 65`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest 4.0.18 |
| **Config file** | `frontend/vite.config.ts` (test block: `{ environment: 'node', globals: true }`) |
| **Quick run command** | `npm run test` (from `frontend/`) |
| **Full suite command** | `npm run test` (from `frontend/`) |
| **Estimated runtime** | ~5 seconds (no test files exist in project src/) |
| **CI pipeline** | None — no CI test pipeline |

---

## Nyquist Sampling Rate

> The minimum feedback frequency required to reliably catch errors in this phase.

- **After every task commit:** Manual browser check of the affected card
- **After every plan wave:** `npm run test` from `frontend/` (confirms no regressions)
- **Before `/gsd:verify-work`:** Manual verification of all 4 success criteria in browser
- **Maximum acceptable task feedback latency:** ~120 seconds (browser reload + visual check)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 65-01-01 | 01 | 1 | ADMOV-01, ADMOV-02, ADMOV-03 | manual-only | n/a — DOM interaction in browser | ❌ No admin test infra | ⬜ pending |
| 65-01-02 | 01 | 1 | ANLYT-01 | manual-only | n/a — third-party dashboard check | ❌ No automated check | ⬜ pending |

*Status values: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

> Test scaffolding committed BEFORE any implementation task. Executor runs Wave 0 first.

Existing infrastructure covers all phase requirements — no Wave 0 test tasks needed.

---

## Manual-Only Verifications

> Behaviors that genuinely cannot be automated, with justification.
> These are surfaced during `/gsd:verify-work` UAT.

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| TopExpertsCard shows all experts when expanded | ADMOV-01 | No admin component test infrastructure; Vitest uses node environment | Click "See All" on Top Experts card, verify full ranked list appears |
| TopQueriesCard shows up to 50 queries when expanded | ADMOV-02 | No admin component test infrastructure; requires live API data | Click "See All" on Top Searches card, verify all queries appear |
| Collapse returns to top 5, period toggle preserved | ADMOV-03 | Stateful UI flow requiring browser interaction | Click "Show less", verify top 5 view; change period toggle while expanded, verify card stays expanded |
| Speed Insights data in Vercel dashboard | ANLYT-01 | Third-party dashboard — cannot be automated | Check Vercel dashboard → Speed Insights tab for frontend deployment |

---

## Validation Sign-Off

Updated by `gsd-plan-checker` when plans are approved:

- [x] All tasks have `<automated>` verify commands or Wave 0 dependencies
- [x] No 3 consecutive implementation tasks without automated verify (sampling continuity)
- [x] Wave 0 test files cover all MISSING references
- [x] No watch-mode flags in any automated command
- [x] Feedback latency per task: < 120s ✅
- [ ] `nyquist_compliant: true` set in frontmatter

**Plan-checker approval:** pending

---

## Execution Tracking

Updated during `/gsd:execute-phase 65`:

| Wave | Tasks | Tests Run | Pass | Fail | Sampling Status |
|------|-------|-----------|------|------|-----------------|
| 1 | 2 | manual | — | — | ⬜ pending |

**Phase validation complete:** pending
