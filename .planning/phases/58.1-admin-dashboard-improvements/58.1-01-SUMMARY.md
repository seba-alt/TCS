---
phase: 58.1-admin-dashboard-improvements
plan: 01
subsystem: api, ui
tags: [fastapi, sqlalchemy, react, admin-dashboard, analytics]

requires:
  - phase: 58-audit-gap-closure
    provides: CORS DELETE fix, currencySymbol consistency
provides:
  - click_count field in /leads API response
  - active_tags field in /analytics-summary recent_searches entries
  - Cleaned Overview page with tag chip rendering
affects: [58.1-03-leads-page, admin-overview]

tech-stack:
  added: []
  patterns:
    - "Batch subquery for cross-table counts (LeadClick -> leads response)"
    - "json_extract for nested payload fields in analytics queries"
    - "Tag chip rendering for tag-only search entries"

key-files:
  created: []
  modified:
    - app/routers/admin/leads.py
    - app/routers/admin/analytics.py
    - frontend/src/admin/types.ts
    - frontend/src/admin/pages/OverviewPage.tsx

key-decisions:
  - "Tag-only searches render as chip badges; empty query + empty tags shows 'Browse (no filter)' italic text"
  - "click_count uses batch subquery on LeadClick grouped by email — avoids N+1 per-lead queries"
  - "active_tags extracted via json_extract from user_events payload — same pattern as query_text and result_count"

patterns-established:
  - "Tag chip component pattern: bg-slate-700/80 border-slate-600/50 rounded-full text-xs"

requirements-completed: [ADMUI-01, ADMUI-02]

duration: 5min
completed: 2026-03-03
---

# Phase 58.1 Plan 01: Backend Extensions + Overview Cleanup Summary

**Added click_count and active_tags to admin API responses; cleaned Overview page by removing Top Feedback/Top Searched sections and rendering tag-only searches as styled tag chips**

## Performance

- **Duration:** 5 min
- **Started:** 2026-03-03
- **Completed:** 2026-03-03
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- Backend /leads endpoint now returns click_count per lead via batch LeadClick subquery
- Backend /analytics-summary returns active_tags array in each recent_searches entry via json_extract
- Overview page reduced from 6 sections to 4 (removed Top Searched Queries and Top Feedback)
- Tag-only explore searches render as styled tag chips instead of blank rows
- All Overview page links updated from /admin/data/searches and /admin/data/marketplace to /admin/data

## Task Commits

1. **Task 1: Extend backend endpoints with click_count and active_tags** - `33e076c` (feat)
2. **Task 2: Clean up Overview page** - `39953c0` (feat)

## Files Created/Modified
- `app/routers/admin/leads.py` - Added click_count via LeadClick batch subquery
- `app/routers/admin/analytics.py` - Added active_tags json_extract to analytics-summary
- `frontend/src/admin/types.ts` - Added active_tags to RecentSearchEntry, click_count to LeadRow
- `frontend/src/admin/pages/OverviewPage.tsx` - Removed sections, added tag chips, updated links

## Decisions Made
- Tag-only searches use tag chip rendering with "tags" label prefix
- Empty query + empty tags shows "Browse (no filter)" in italic
- Removed Top Searched Queries and Top Feedback as user considers them irrelevant

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- click_count available in /leads response for Plan 58.1-03 (Leads page click column)
- Overview page cleaned up and ready for production

---
*Phase: 58.1-admin-dashboard-improvements*
*Completed: 2026-03-03*
