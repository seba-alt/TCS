---
phase: 74
slug: analytics-hardening
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-05
---

# Phase 74 — Validation Strategy

> Generated by `gsd-phase-researcher` during `/gsd:plan-phase 74`.
> Updated by `gsd-plan-checker` after plan approval.
> Governs feedback sampling during `/gsd:execute-phase 74`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest (via vite.config.ts) |
| **Config file** | frontend/vite.config.ts (test section) |
| **Quick run command** | `cd frontend && npx vitest run src/tracking.test.ts` |
| **Full suite command** | `cd frontend && npm test` |
| **Estimated runtime** | ~5 seconds |
| **CI pipeline** | None — local dev only |

---

## Nyquist Sampling Rate

> The minimum feedback frequency required to reliably catch errors in this phase.

- **After every task commit:** Run `cd frontend && npx vitest run src/tracking.test.ts`
- **After every plan wave:** Run `cd frontend && npm test`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Maximum acceptable task feedback latency:** 5 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 74-01-01 | 01 | 1 | ANLT-01, ANLT-04 | manual-only | N/A (HTML changes) | N/A | pending |
| 74-01-02 | 01 | 1 | ANLT-02 | unit | `cd frontend && npx vitest run src/tracking.test.ts` | Needs new tests | pending |
| 74-01-03 | 01 | 1 | ANLT-03 | unit | `cd frontend && npx vitest run src/tracking.test.ts` | Needs new tests | pending |

*Status values: pending / green / red / flaky*

---

## Wave 0 Requirements

> Test scaffolding committed BEFORE any implementation task. Executor runs Wave 0 first.

Existing infrastructure covers all phase requirements — no Wave 0 test tasks needed. New test cases will be added to the existing `frontend/src/tracking.test.ts` file as part of implementation tasks.

---

## Manual-Only Verifications

> Behaviors that genuinely cannot be automated, with justification.
> These are surfaced during `/gsd:verify-work` UAT.

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| GA4 DebugView shows exactly one page_view on fresh load | ANLT-01 | Requires live GA4 DebugView in Google Analytics console | Open GA4 DebugView, load page, verify single page_view event |
| transport_type: beacon visible in gtag config | ANLT-01 | HTML config verification | Inspect index.html source, confirm transport_type: 'beacon' present |
| Inline comment on send_page_view: false | ANLT-04 | HTML comment verification | Inspect index.html source, confirm comment present |

---

## Validation Sign-Off

Updated by `gsd-plan-checker` when plans are approved:

- [ ] All tasks have automated verify commands or Wave 0 dependencies
- [ ] No 3 consecutive implementation tasks without automated verify (sampling continuity)
- [ ] Wave 0 test files cover all MISSING references
- [ ] No watch-mode flags in any automated command
- [ ] Feedback latency per task: < 5s
- [ ] `nyquist_compliant: true` set in frontmatter

**Plan-checker approval:** pending

---

## Execution Tracking

Updated during `/gsd:execute-phase 74`:

| Wave | Tasks | Tests Run | Pass | Fail | Sampling Status |
|------|-------|-----------|------|------|-----------------|
| 1 | 3 | `cd frontend && npx vitest run src/tracking.test.ts` | — | — | pending |

**Phase validation complete:** pending
