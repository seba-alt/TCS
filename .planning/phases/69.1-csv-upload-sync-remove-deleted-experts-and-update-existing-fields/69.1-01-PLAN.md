---
phase: 69.1-csv-upload-sync-remove-deleted-experts-and-update-existing-fields
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models.py
  - app/services/explorer.py
  - app/routers/browse.py
  - app/routers/admin/experts.py
  - app/routers/admin/_common.py
  - scripts/ingest.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "Soft-deleted experts do not appear in public search results"
    - "Soft-deleted experts do not appear in the Browse page"
    - "Soft-deleted experts are excluded from the FAISS index on rebuild"
    - "Admin expert list includes is_active field and supports filtering"
    - "Existing experts remain active (is_active defaults to True)"
  artifacts:
    - path: "app/models.py"
      provides: "Expert.is_active Boolean column with default True and server_default '1'"
      contains: "is_active"
    - path: "app/services/explorer.py"
      provides: "Explorer pre-filter excludes inactive experts"
      contains: "is_active"
    - path: "app/routers/browse.py"
      provides: "Browse queries filter to active-only experts"
      contains: "is_active"
    - path: "scripts/ingest.py"
      provides: "FAISS ingest only indexes active experts"
      contains: "is_active"
  key_links:
    - from: "app/services/explorer.py"
      to: "app/models.py"
      via: "Expert.is_active filter in pre-filter WHERE clause"
      pattern: "Expert\\.is_active"
    - from: "app/routers/browse.py"
      to: "app/models.py"
      via: "Expert.is_active filter in all browse queries"
      pattern: "Expert\\.is_active"
---

<objective>
Add soft-delete infrastructure to the Expert model and filter inactive experts from all public-facing queries.

Purpose: Foundation for CSV sync — experts removed from CSV will be soft-deleted (is_active=False) rather than hard-deleted, preserving historical data while hiding them from public search/browse.

Output: Expert model with is_active column; all public queries filter to active-only; admin list exposes is_active field; FAISS ingest excludes inactive experts.
</objective>

<execution_context>
@/Users/sebastianhamers/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sebastianhamers/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/models.py
@app/services/explorer.py
@app/routers/browse.py
@app/routers/admin/experts.py
@app/routers/admin/_common.py
@scripts/ingest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add is_active column to Expert model and filter all public queries</name>
  <files>
    app/models.py
    app/services/explorer.py
    app/routers/browse.py
    scripts/ingest.py
  </files>
  <action>
1. In `app/models.py`, add to the Expert class:
   ```python
   is_active: Mapped[bool] = mapped_column(Boolean, nullable=False, default=True, server_default="1")
   ```
   Place it after `industry_tags` and before `created_at`.

2. In `app/services/explorer.py`, function `run_explore()`:
   - In the saved-view shortcut (around line 197), add `.where(Expert.is_active == True)` to the `select(Expert).where(Expert.username.in_(usernames))` query. This ensures saved views don't return inactive experts.
   - In the Stage 1 pre-filter (around line 213), add `Expert.is_active == True` to the `and_()` clause alongside the hourly_rate filters. This ensures ALL search results exclude inactive experts.

3. In `app/routers/browse.py`, add `Expert.is_active == True` filter to ALL Expert queries:
   - `_build_browse_data()` category counts query (line 56): add `.where(Expert.is_active == True)` before `.group_by()`
   - Category experts query (line 68): add `.where(Expert.is_active == True)` alongside the category filter
   - Recently Added total count (line 83): add `.where(Expert.is_active == True)` to select_from
   - Recently Added query (line 87): add `.where(Expert.is_active == True)`
   - Cold-start "All Experts" total (line 104): add `.where(Expert.is_active == True)`
   - Cold-start "All Experts" query (line 108): add `.where(Expert.is_active == True)`
   - Featured experts (line 122): add `.where(Expert.is_active == True)`
   - Photo proxy (line 167): NO filter needed — photos should still work for inactive experts (admin may view them)

4. In `scripts/ingest.py`, function `load_tagged_experts()`:
   - Change the query from `select(Expert).where(Expert.tags.isnot(None))` to `select(Expert).where(Expert.tags.isnot(None), Expert.is_active == True)` so FAISS index excludes inactive experts.
  </action>
  <verify>
    <automated>cd /Users/sebastianhamers/Documents/TCS && python -c "
from app.models import Expert
col = Expert.__table__.c.is_active
assert col is not None, 'is_active column missing'
assert str(col.server_default.arg) == '1', 'server_default should be 1'
print('OK: is_active column exists with correct defaults')
"</automated>
    <manual>Verify is_active filter appears in explorer.py, browse.py, and ingest.py</manual>
  </verify>
  <done>Expert model has is_active Boolean column defaulting True; all public search/browse/FAISS queries filter to active-only; photo proxy remains unfiltered.</done>
</task>

<task type="auto">
  <name>Task 2: Update admin expert endpoints to expose is_active and support filtering</name>
  <files>
    app/routers/admin/experts.py
    app/routers/admin/_common.py
  </files>
  <action>
1. In `app/routers/admin/_common.py`, update `_serialize_expert()` to include `is_active`:
   ```python
   def _serialize_expert(e: Expert) -> dict:
       return {
           ...existing fields...,
           "is_active": e.is_active,
       }
   ```
   Add `"is_active": e.is_active` after `"industry_tags"` in the return dict.

2. In `app/routers/admin/experts.py`, update the `get_experts()` endpoint (GET /experts):
   - Add an optional query parameter `active_only: bool = Query(default=False)`.
   - When `active_only=True`, add `.where(Expert.is_active == True)` to the query.
   - This lets the admin frontend toggle between all experts and active-only.
   - Import `Query` from fastapi (add to the existing import line).

3. In `app/routers/admin/experts.py`, update the existing hard-delete endpoints to use soft-delete instead:
   - `delete_expert()` (DELETE /experts/{username}): Instead of `db.delete(expert)`, set `expert.is_active = False` and `db.commit()`. Remove the metadata.json and experts.csv cleanup code (FAISS rebuild will handle exclusion). Keep the FAISS rebuild trigger.
   - `delete_experts_bulk()` (POST /experts/delete-bulk): Same pattern — set `is_active = False` for each expert instead of `db.delete()`. Remove metadata.json and experts.csv cleanup. Keep FAISS rebuild trigger.
   - Note: The response format stays the same (`{"ok": True, "deleted": N, ...}`) for backward compatibility.
  </action>
  <verify>
    <automated>cd /Users/sebastianhamers/Documents/TCS && python -c "
from app.routers.admin._common import _serialize_expert
from app.models import Expert
e = Expert(username='test', is_active=True)
s = _serialize_expert(e)
assert 'is_active' in s, 'is_active missing from serialized expert'
assert s['is_active'] == True, 'is_active should be True'
print('OK: _serialize_expert includes is_active')
"</automated>
    <manual>Check that delete endpoints now soft-delete instead of hard-delete</manual>
  </verify>
  <done>Admin GET /experts returns is_active field; supports active_only query param; DELETE endpoints soft-delete (set is_active=False) instead of hard-delete.</done>
</task>

</tasks>

<verification>
1. Expert model has `is_active` column with Boolean type, default True, server_default "1"
2. `run_explore()` in explorer.py filters on `Expert.is_active == True` in both saved-view and pre-filter paths
3. All browse.py Expert queries (7 queries) include `is_active == True` filter (except photo proxy)
4. `load_tagged_experts()` in ingest.py includes `is_active == True` filter
5. `_serialize_expert()` includes `is_active` field
6. Admin GET /experts supports `active_only` query parameter
7. DELETE endpoints use soft-delete pattern (set is_active=False)
</verification>

<success_criteria>
- Inactive experts are excluded from all public-facing pages (search, browse, FAISS index)
- Admin can see all experts including inactive ones
- Admin can filter to active-only
- Delete operations soft-delete rather than hard-delete
- Existing experts remain active (no data migration needed — server_default handles it)
</success_criteria>

<output>
After completion, create `.planning/phases/69.1-csv-upload-sync-remove-deleted-experts-and-update-existing-fields/69.1-01-SUMMARY.md`
</output>
