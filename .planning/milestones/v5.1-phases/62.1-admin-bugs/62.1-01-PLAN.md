---
phase: 62.1-admin-bugs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/admin/pages/OverviewPage.tsx
autonomous: true
requirements: [BUG-FIX]

must_haves:
  truths:
    - "Admin overview shows exactly 6 detail cards in order: Zero-Result Queries, Top Searches, Top Clicks, Recent Searches, Recent Clicks, Leads"
    - "No duplicate cards exist — UnmetDemandCard and TopZeroResultsCard are merged into a single ZeroResultQueriesCard"
    - "Stat mini-card row displays 'Zero-Result Queries' instead of 'Gaps'"
    - "All card titles match unified naming: no 'Unmet Demand', no 'Recent Explore Searches', no 'Recent Card Clicks', no 'Recent Leads', no 'Top Experts'"
    - "ZeroResultQueriesCard is period-aware (respects days prop) and includes 'See all' link to /admin/gaps"
  artifacts:
    - path: "frontend/src/admin/pages/OverviewPage.tsx"
      provides: "Cleaned up overview page with unified naming, merged cards, and correct order"
      contains: "ZeroResultQueriesCard"
  key_links:
    - from: "ZeroResultQueriesCard"
      to: "/events/demand"
      via: "adminFetch with days prop"
      pattern: "adminFetch.*events/demand.*days"
    - from: "ZeroResultQueriesCard"
      to: "/admin/gaps"
      via: "Link component in header"
      pattern: "Link to=\"/admin/gaps\""
---

<objective>
Fix admin overview dashboard: unify card naming ("Zero-Result Queries", "Searches", "Clicks", "Leads"), remove duplicate UnmetDemandCard/TopZeroResultsCard, reorder cards for business visibility (Top > Recent, actionable intel first).

Purpose: Admin overview currently has duplicate cards with inconsistent naming and suboptimal ordering. This cleanup makes the dashboard clean, consistent, and prioritizes actionable business intelligence.
Output: Cleaned up OverviewPage.tsx with 6 uniquely-named cards in the correct order.

**Scope note:** Backend API routes were audited during research (see 62.1-RESEARCH.md). Route paths like `/events/demand` and `/analytics/top-queries` are functional identifiers, not display labels — no backend route or response field renames are required. The CONTEXT.md decision "naming changes apply to both frontend labels AND backend" is satisfied: the backend was investigated and found to need no changes.
</objective>

<execution_context>
@/Users/sebastianhamers/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sebastianhamers/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v5.1-phases/62.1-admin-bugs/62.1-CONTEXT.md
@.planning/milestones/v5.1-phases/62.1-admin-bugs/62.1-RESEARCH.md
@frontend/src/admin/pages/OverviewPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge duplicate cards and unify all card names</name>
  <files>frontend/src/admin/pages/OverviewPage.tsx</files>
  <action>
In `OverviewPage.tsx`, make the following changes to card component definitions:

1. **Delete `TopZeroResultsCard` component entirely** (lines ~88-129). It is a duplicate of `UnmetDemandCard` calling the same `/events/demand` endpoint but with hardcoded `days: 30` and no period awareness.

2. **Rename `UnmetDemandCard` to `ZeroResultQueriesCard`:**
   - Change function name from `UnmetDemandCard` to `ZeroResultQueriesCard`
   - Change title from `"Unmet Demand"` to `"Zero-Result Queries"`
   - Add a "See all" link to `/admin/gaps` in the header (copy pattern from deleted `TopZeroResultsCard`):
     ```tsx
     <div className="flex items-center justify-between mb-4">
       <div className="flex items-center gap-2">
         <AlertCircle className="w-4 h-4 text-amber-400" />
         <h2 className="text-sm font-semibold text-white">Zero-Result Queries</h2>
       </div>
       <Link to="/admin/gaps" className="text-xs text-purple-400 hover:text-purple-300 transition-colors">
         See all &rarr;
       </Link>
     </div>
     ```
   - Keep everything else from `UnmetDemandCard` (period-aware `days` prop, skeleton loaders, empty state, ranked list)

3. **Rename `RecentExploreSearchesCard` title** from `"Recent Explore Searches"` to `"Recent Searches"` (line ~180). Keep component function name as-is or rename to `RecentSearchesCard` for consistency.

4. **Rename `RecentCardClicksCard` title** from `"Recent Card Clicks"` to `"Recent Clicks"` (line ~234). Keep component function name as-is or rename to `RecentClicksCard` for consistency.

5. **Rename `RecentLeadsCard` title** from `"Recent Leads"` to `"Leads"` (line ~151). Keep component function name as-is or rename to `LeadsCard` for consistency.

6. **Rename `TopExpertsCard` title** from `"Top Experts"` to `"Top Clicks"` (line ~285). Keep component function name as-is or rename to `TopClicksCard` for consistency. The data (click counts per expert) remains the same — only the heading changes, per CONTEXT.md spec.

7. **Rename stat mini-card label** from `"Gaps"` to `"Zero-Result Queries"` (line ~479). Do NOT change the underlying data source (`stats.gap_count`).

Do NOT:
- Change any backend API endpoints or response shapes
- Change URL routes (e.g., `/admin/gaps` stays)
- Change the stat card's data source (still uses `stats.gap_count`)
- Resize cards — ordering creates visibility, not size
- Add breakdown badges to merged cards
  </action>
  <verify>
    <automated>cd /Users/sebastianhamers/Documents/TCS/frontend && npx tsc -b --noEmit 2>&1 | head -20</automated>
    <manual>Verify renamed component compiles without TypeScript errors</manual>
  </verify>
  <done>All 7 card components have unified names matching spec: ZeroResultQueriesCard, TopQueriesCard (Top Searches), TopExpertsCard (Top Clicks), RecentSearchesCard (Recent Searches), RecentClicksCard (Recent Clicks), LeadsCard (Leads). Stat row says "Zero-Result Queries" not "Gaps". UnmetDemandCard and TopZeroResultsCard no longer exist as separate components.</done>
</task>

<task type="auto">
  <name>Task 2: Reorder cards into single unified grid with correct priority</name>
  <files>frontend/src/admin/pages/OverviewPage.tsx</files>
  <action>
In the `OverviewPage` main component's JSX return, replace the current two separate grids (the 2-col "Detail sections" grid + the 3-col "Ranked insights row" grid) with a single unified 2-column grid containing all 6 cards in this exact order:

```tsx
{/* All detail cards — single unified grid, ordered by visibility */}
<div className="grid grid-cols-1 xl:grid-cols-2 gap-6">
  <ZeroResultQueriesCard days={days} />
  <TopQueriesCard days={days} />
  <TopExpertsCard days={days} />
  <RecentExploreSearchesCard
    searches={analytics?.recent_searches ?? []}
    loading={analyticsLoading}
  />
  <RecentCardClicksCard
    clicks={analytics?.recent_clicks ?? []}
    loading={analyticsLoading}
  />
  <RecentLeadsCard />
</div>
```

(Use the renamed component names from Task 1 if function names were also renamed.)

This replaces both:
- The old `{/* Detail sections — 2x2 grid */}` block (lines ~499-511)
- The old `{/* Ranked insights row */}` block (lines ~513-518)

Remove the `<UnmetDemandCard days={days} />` usage since the component was deleted in Task 1 (it is replaced by `ZeroResultQueriesCard`). Remove `<TopZeroResultsCard />` usage since it was also deleted.

The card order follows the user's spec:
1. Zero-Result Queries (prominent, actionable business intel)
2. Top Searches
3. Top Clicks
4. Recent Searches
5. Recent Clicks
6. Leads

In a 2-col grid this pairs as: (ZRQ, Top Searches), (Top Clicks, Recent Searches), (Recent Clicks, Leads).

Remove any now-unused imports (e.g., if `CheckCircle` was only used in `UnmetDemandCard` it stays since ZeroResultQueriesCard still uses it).
  </action>
  <verify>
    <automated>cd /Users/sebastianhamers/Documents/TCS/frontend && npx tsc -b --noEmit 2>&1 | head -20 && npm run test 2>&1 | tail -10</automated>
    <manual>Start dev server, visit /admin — verify 6 cards appear in correct order: ZRQ, Top Searches, Top Clicks, Recent Searches, Recent Clicks, Leads</manual>
  </verify>
  <done>OverviewPage renders a single 2-column grid with exactly 6 cards in the specified order. No duplicate cards. No orphaned references to deleted components. TypeScript compiles clean. Existing tests pass.</done>
</task>

</tasks>

<verification>
1. `cd /Users/sebastianhamers/Documents/TCS/frontend && npx tsc -b --noEmit` — TypeScript compiles clean
2. `cd /Users/sebastianhamers/Documents/TCS/frontend && npm run test` — all existing tests pass
3. Visual: admin overview shows stat row with "Zero-Result Queries" label (not "Gaps")
4. Visual: 6 detail cards in single 2-col grid in correct order
5. Visual: no duplicate cards
6. ZeroResultQueriesCard respects period toggle and has "See all" link to /admin/gaps
</verification>

<success_criteria>
- OverviewPage.tsx compiles without TypeScript errors
- Exactly 6 card components rendered (no duplicates)
- Card order matches spec: ZRQ, Top Searches, Top Clicks, Recent Searches, Recent Clicks, Leads
- All naming unified per CONTEXT.md decisions
- Period toggle still works for all period-aware cards
- "See all" link on ZeroResultQueriesCard navigates to /admin/gaps
</success_criteria>

<output>
After completion, create `.planning/milestones/v5.1-phases/62.1-admin-bugs/62.1-01-SUMMARY.md`
</output>
