---
phase: 59-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/admin/pages/LeadsPage.tsx
  - frontend/src/admin/pages/AdminMarketplacePage.tsx
  - frontend/src/admin/components/GapsTable.tsx
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - ADMUI-01
  - ADMUI-02
  - ADMUI-03
  - ADMUI-04

must_haves:
  truths:
    - "LeadsPage no longer has a 'Searches ->' button or any navigation passing email state to DataPage"
    - "LeadsPage no longer reads location.state or highlights rows based on email state"
    - "ADMUI-03 row in REQUIREMENTS.md traceability table shows Phase 58.1 and status 'N/A -- Sage data source retired'"
    - "ADMUI-01, ADMUI-02, ADMUI-04 rows in REQUIREMENTS.md traceability table show Phase 58.1 and status Complete"
    - "AdminMarketplacePage.tsx no longer exists on disk"
    - "GapsTable no longer navigates to the non-existent /admin/searches route"
  artifacts:
    - path: "frontend/src/admin/pages/LeadsPage.tsx"
      provides: "Clean leads page without dead email-to-data navigation"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Complete ADMUI traceability entries with correct statuses"
      contains: "N/A -- Sage data source retired"
  key_links:
    - from: "frontend/src/admin/pages/LeadsPage.tsx"
      to: "frontend/src/admin/pages/DataPage.tsx"
      via: "No navigate call — dead link removed"
      pattern: "navigate.*admin/data.*state"
---

<objective>
Resolve all v5.0 tech debt identified by the milestone re-audit: remove the dead LeadsPage email handoff, close ADMUI-03 as N/A, verify ADMUI traceability entries, delete orphaned AdminMarketplacePage.tsx, and fix the dead GapsTable searches link.

Purpose: The v5.0 milestone re-audit found dead code paths (button navigating with unused state), a partial requirement (ADMUI-03 for Sage data source that was retired), and an orphaned file. This plan cleans all of it up so the codebase matches the documentation.

Output: Clean LeadsPage, deleted orphan, corrected traceability table, fixed GapsTable link.
</objective>

<execution_context>
@/Users/sebastianhamers/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sebastianhamers/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-tech-debt-cleanup/59-RESEARCH.md

@frontend/src/admin/pages/LeadsPage.tsx
@frontend/src/admin/pages/AdminMarketplacePage.tsx
@frontend/src/admin/components/GapsTable.tsx
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead "Searches ->" button and email state from LeadsPage</name>
  <files>
    frontend/src/admin/pages/LeadsPage.tsx
  </files>
  <action>
    Read `LeadsPage.tsx` fully before making changes.

    **Remove the "Searches ->" button column:**
    1. Delete the entire `<td>` block (lines ~315-325) containing the button that calls `navigate('/admin/data', { state: { email: lead.email } })`
    2. Delete the empty header `<th className="px-5 py-3" />` (line ~249) that served as the column header for this button

    **Remove dead email state consumption:**
    1. Remove `const highlightEmail: string = location.state?.email ?? ''` (line ~26)
    2. Simplify `const [expandedEmail, setExpandedEmail] = useState<string | null>(highlightEmail || null)` to `const [expandedEmail, setExpandedEmail] = useState<string | null>(null)`
    3. Remove the `highlightRef` declaration: `const highlightRef = useRef<HTMLTableRowElement | null>(null)` (line ~29)
    4. Remove the `useEffect` that scrolls to the highlighted row (lines ~75-79)
    5. Remove all `isHighlighted` logic in the table rows: the `isHighlighted` variable, the conditional `ref={isHighlighted ? highlightRef : null}`, the purple highlight styling, and the "from search" badge `<span>` (lines ~261-294 area)
    6. The row styling should become simply: `className="border-b border-slate-700/40 cursor-pointer transition-colors hover:bg-slate-700/20"`

    **Update colSpan values:**
    1. Find `colSpan={6}` in the empty state row and expanded row — change both to `colSpan={5}` (since we removed one column)

    **Clean up imports:**
    1. Remove `useNavigate` from the react-router-dom import (no longer used — `downloadLeadsCsv` and `downloadNewsletterCsv` use `fetch` directly, not navigate)
    2. Remove `useLocation` from the react-router-dom import (only was used for location.state)
    3. Remove `useRef` from the React import if no longer used elsewhere in the component (check first)
    4. Keep the `useNavigate` import ONLY if something else in the component still calls navigate — check carefully

    **No commented code, no TODOs, no backward-compat shims** — clean removal per user decision.
  </action>
  <verify>
    <automated>cd /Users/sebastianhamers/Documents/TCS/frontend && npx vite build 2>&1 | tail -5</automated>
    <manual>Verify LeadsPage.tsx has no reference to navigate('/admin/data'), no location.state, no highlightEmail, no highlightRef</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>
    - LeadsPage has no "Searches ->" button
    - LeadsPage does not import useNavigate or useLocation
    - LeadsPage does not read location.state
    - LeadsPage has no highlight/scroll-to behavior for email state
    - Table has 5 columns (Email, Searches, Last Active, Gaps, Clicks) — no empty action column
    - Build succeeds without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete orphaned AdminMarketplacePage.tsx and fix GapsTable dead link</name>
  <files>
    frontend/src/admin/pages/AdminMarketplacePage.tsx
    frontend/src/admin/components/GapsTable.tsx
  </files>
  <action>
    **Delete AdminMarketplacePage.tsx:**
    1. Delete `frontend/src/admin/pages/AdminMarketplacePage.tsx` entirely
    2. Verify no file imports it — `main.tsx` line 26 already has a comment saying it was removed; no lazy import exists. The file is truly orphaned.

    **Fix GapsTable dead "View Searches" link:**
    1. Read `frontend/src/admin/components/GapsTable.tsx`
    2. Find the button at line ~77 that navigates to `/admin/searches` with `{ state: { query: gap.query } }` — this route does not exist (hits catch-all redirect to /admin)
    3. Remove the "View Searches ->" button entirely — it navigates to a non-existent route
    4. Keep the "Mark Resolved" button as-is
    5. After removing the View Searches button, the `<div className="flex items-center gap-4">` wrapper may be unnecessary if only one button remains. Simplify: if only "Mark Resolved" remains, remove the flex wrapper div and just render the button directly in the `<td>`
    6. Check if `useNavigate` import is still needed by GapsTable after this removal — if not, remove it
  </action>
  <verify>
    <automated>cd /Users/sebastianhamers/Documents/TCS/frontend && npx vite build 2>&1 | tail -5</automated>
    <manual>Verify AdminMarketplacePage.tsx no longer exists: ls frontend/src/admin/pages/AdminMarketplacePage.tsx should fail. Verify GapsTable has no /admin/searches navigation.</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>
    - AdminMarketplacePage.tsx deleted from disk
    - GapsTable no longer has "View Searches ->" button
    - GapsTable no longer navigates to /admin/searches
    - Build succeeds without errors
  </done>
</task>

<task type="auto">
  <name>Task 3: Update ADMUI-03 traceability and verify ADMUI entries</name>
  <files>
    .planning/REQUIREMENTS.md
  </files>
  <action>
    Read `.planning/REQUIREMENTS.md` fully.

    **Update ADMUI-03 entry:**
    1. Find the row `| ADMUI-03 | Phase 59 | Pending |` in the traceability table
    2. Change it to: `| ADMUI-03 | Phase 58.1 | N/A -- Sage data source retired |`
    3. This reflects that ADMUI-03 (Searches + Marketplace merge) was addressed in Phase 58.1 but the searches table was removed because the Sage data source was retired. The requirement is formally closed as N/A.

    **Verify existing ADMUI entries are correct:**
    1. Confirm `| ADMUI-01 | Phase 58.1 | Complete |` is present and correct
    2. Confirm `| ADMUI-02 | Phase 58.1 | Complete |` is present and correct
    3. Confirm `| ADMUI-04 | Phase 58.1 | Complete |` is present and correct
    4. If any are incorrect, fix them

    **Update coverage count if needed:**
    - The "Coverage" section at the bottom should reflect the correct total. Currently shows 23 total. ADMUI-01-04 are already counted. No change needed unless the count is wrong.

    **Update "Last updated" line** at the bottom of the file to reflect today's date and this phase.
  </action>
  <verify>
    <automated>grep "ADMUI-03" .planning/REQUIREMENTS.md</automated>
    <manual>Verify ADMUI-03 row shows "Phase 58.1 | N/A -- Sage data source retired" and all 4 ADMUI rows are correct</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>
    - ADMUI-03 shows Phase 58.1 with status "N/A -- Sage data source retired"
    - ADMUI-01, ADMUI-02, ADMUI-04 show Phase 58.1 with status "Complete"
    - All 4 ADMUI requirements have correct entries in the traceability table
    - "Last updated" line reflects current date
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npx vite build` — frontend builds without errors
2. `ls frontend/src/admin/pages/AdminMarketplacePage.tsx` — file should NOT exist (exit code 1)
3. `grep -c "navigate.*admin/data.*state" frontend/src/admin/pages/LeadsPage.tsx` — should return 0
4. `grep -c "admin/searches" frontend/src/admin/components/GapsTable.tsx` — should return 0
5. `grep "ADMUI-03" .planning/REQUIREMENTS.md` — should show "N/A -- Sage data source retired"
6. `grep -c "location.state" frontend/src/admin/pages/LeadsPage.tsx` — should return 0
</verification>

<success_criteria>
- LeadsPage has no "Searches ->" button, no email state passing, no highlight behavior
- AdminMarketplacePage.tsx deleted from disk
- GapsTable no longer navigates to non-existent /admin/searches route
- ADMUI-03 formally closed as N/A in traceability table
- All 4 ADMUI requirements tracked correctly in REQUIREMENTS.md
- Frontend builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/59-tech-debt-cleanup/59-01-SUMMARY.md`
</output>
