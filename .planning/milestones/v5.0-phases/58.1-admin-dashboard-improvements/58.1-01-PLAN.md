---
phase: 58.1-admin-dashboard-improvements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/routers/admin/leads.py
  - app/routers/admin/analytics.py
  - frontend/src/admin/types.ts
  - frontend/src/admin/hooks/useAdminData.ts
  - frontend/src/admin/pages/OverviewPage.tsx
autonomous: true
requirements:
  - ADMUI-01
  - ADMUI-02

must_haves:
  truths:
    - "Tag-only explore searches render as tag chips in the Overview Recent Explore Searches card instead of blank rows"
    - "The Overview page no longer shows the Top Feedback section or the Top Searched Queries section"
    - "The /leads endpoint returns a click_count integer field for each lead"
    - "The /analytics-summary endpoint returns active_tags array in each recent_searches entry"
    - "Overview Recent Card Clicks card shows lead email alongside expert name, search query, timestamp, and source"
  artifacts:
    - path: "app/routers/admin/leads.py"
      provides: "click_count field in leads response via LeadClick subquery"
      contains: "click_count"
    - path: "app/routers/admin/analytics.py"
      provides: "active_tags extracted from event payload in analytics-summary"
      contains: "active_tags"
    - path: "frontend/src/admin/types.ts"
      provides: "Updated TypeScript types for RecentSearchEntry.active_tags and LeadRow.click_count"
      contains: "active_tags"
    - path: "frontend/src/admin/pages/OverviewPage.tsx"
      provides: "Tag chip rendering, removed sections, enriched click card"
  key_links:
    - from: "app/routers/admin/analytics.py"
      to: "user_events payload"
      via: "json_extract for active_tags"
      pattern: "json_extract.*active_tags"
    - from: "frontend/src/admin/pages/OverviewPage.tsx"
      to: "frontend/src/admin/types.ts"
      via: "RecentSearchEntry.active_tags used for chip rendering"
      pattern: "active_tags"
---

<objective>
Add active_tags and click_count to backend responses; clean up the Overview page by removing low-value sections and rendering tag-only searches as chips.

Purpose: The Overview page currently shows blank rows for tag-only searches (because query_text is empty), has two irrelevant sections (Top Feedback, Top Searched Queries), and click activity lacks email/query context. The backend needs to surface active_tags and click_count for the frontend to display richer data.

Output: Two backend endpoint extensions + a cleaned-up, enriched Overview page.
</objective>

<execution_context>
@/Users/sebastianhamers/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sebastianhamers/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58.1-admin-dashboard-improvements/58.1-RESEARCH.md

@app/routers/admin/leads.py
@app/routers/admin/analytics.py
@frontend/src/admin/types.ts
@frontend/src/admin/hooks/useAdminData.ts
@frontend/src/admin/pages/OverviewPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend backend endpoints with click_count and active_tags</name>
  <files>
    app/routers/admin/leads.py
    app/routers/admin/analytics.py
    frontend/src/admin/types.ts
    frontend/src/admin/hooks/useAdminData.ts
  </files>
  <action>
    **In `app/routers/admin/leads.py` — add click_count to /leads response:**
    1. Import `func`, `select` from sqlalchemy and `LeadClick` from `app.models`
    2. In `get_leads()`, before the main loop, run a subquery to batch-count clicks per email:
       ```python
       click_counts_rows = db.execute(
           select(LeadClick.email, func.count(LeadClick.id).label("click_count"))
           .group_by(LeadClick.email)
       ).all()
       click_map = {r.email: r.click_count for r in click_counts_rows}
       ```
    3. In the lead dict construction, add `"click_count": click_map.get(email, 0)`

    **In `app/routers/admin/analytics.py` — add active_tags to /analytics-summary:**
    1. Add `import json` at top if not present
    2. In the `get_analytics_summary()` function, find the SQL query for `recent_search_rows` that selects from `user_events WHERE event_type = 'search_query'`
    3. Add `json_extract(payload, '$.active_tags') AS active_tags` to the SELECT clause
    4. In the result dict comprehension, add:
       ```python
       "active_tags": json.loads(r.active_tags) if r.active_tags else []
       ```

    **In `frontend/src/admin/types.ts`:**
    1. Add `active_tags?: string[]` to the `RecentSearchEntry` interface
    2. Add `click_count: number` to the `LeadRow` interface (or whatever the lead type is called — check file)

    **In `frontend/src/admin/hooks/useAdminData.ts`:**
    - No changes needed unless the hook needs to pass through the new fields. The existing `useAnalyticsSummary` and `useLeads` hooks should auto-include the new response fields since they use generic fetch. Verify this by reading the hook code — if it destructures specific fields, update it.
  </action>
  <verify>
    <automated>cd /Users/sebastianhamers/Documents/TCS && python -c "from app.routers.admin.leads import router; print('leads.py imports OK')" && python -c "from app.routers.admin.analytics import router; print('analytics.py imports OK')"</automated>
    <manual>Verify click_count appears in LeadRow type and active_tags appears in RecentSearchEntry type</manual>
  </verify>
  <done>
    - GET /leads response includes click_count: number for each lead
    - GET /analytics-summary response includes active_tags: string[] in each recent_searches entry
    - TypeScript types updated to match new response shapes
  </done>
</task>

<task type="auto">
  <name>Task 2: Clean up Overview page — remove sections, render tag chips, enrich click card</name>
  <files>
    frontend/src/admin/pages/OverviewPage.tsx
  </files>
  <action>
    Read `OverviewPage.tsx` fully before making changes.

    **Remove sections (per user: "top feedback is irrelevant, top searched is empty"):**
    1. Find and remove the "Top Searched Queries" section entirely (the one reading `top_queries` from `/stats`)
    2. Find and remove the "Top Feedback" section entirely
    3. This should remove the bottom row, leaving 4 stat cards at top + the 2x2 detail cards grid

    **Fix Recent Explore Searches card — render tag chips:**
    1. In the Recent Explore Searches card, find the row rendering for each search entry
    2. When `row.query_text` is empty (or falsy) and `row.active_tags` has items, render each tag as a chip instead of showing blank text:
       ```tsx
       {isTagSearch ? (
         (row.active_tags ?? []).map((tag, ti) => (
           <span key={ti} className="inline-flex items-center px-2 py-0.5 rounded-full bg-slate-700/80 border border-slate-600/50 text-xs text-slate-300">
             {tag}
           </span>
         ))
       ) : (
         <span className="text-sm text-slate-300 truncate">{row.query_text}</span>
       )}
       ```
    3. Add a small visual indicator for tag searches — e.g., a "tags" label in `text-[10px] text-slate-500 uppercase tracking-wider` next to the result count
    4. When `query_text` is a regular text search, keep current rendering but add a subtle search icon or "query" label to distinguish from tag searches
    5. Skip rows where both `query_text` is empty AND `active_tags` is empty/undefined — show nothing or "Browse (no filter)"

    **Enrich Recent Card Clicks card:**
    1. The card currently shows expert name + source + time. Check what fields are available in the `recent_clicks` data from `analytics-summary`
    2. If `email` and `search_query` are available in the response, add them to each click row so the admin sees: lead email, expert name, search query context, timestamp, source
    3. If fields are missing from the API response, show what's available and add a TODO comment

    **Empty state handling:**
    - For Recent Explore Searches: if `searches.length === 0`, show `<p className="text-slate-500 text-sm">No searches in this period</p>`
    - For Recent Card Clicks: if `clicks.length === 0`, show `<p className="text-slate-500 text-sm">No click activity in this period</p>`
    - For other cards: add similar context-specific empty messages where missing
  </action>
  <verify>
    <automated>cd /Users/sebastianhamers/Documents/TCS/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Check that OverviewPage no longer has Top Feedback or Top Searched sections, and that tag-only search rows render as chips</manual>
  </verify>
  <done>
    - Overview page has 4 stat cards + 2x2 detail cards (no Top Feedback, no Top Searched)
    - Tag-only searches display as styled tag chips instead of blank rows
    - Text searches and tag searches are visually distinguishable
    - Recent Card Clicks card shows maximum available context per click row
    - All sections have context-specific empty state messages
  </done>
</task>

</tasks>

<verification>
1. `cd /Users/sebastianhamers/Documents/TCS/frontend && npx tsc --noEmit` — TypeScript compiles without errors
2. `cd /Users/sebastianhamers/Documents/TCS/frontend && npm run test` — existing tests pass
3. `cd /Users/sebastianhamers/Documents/TCS && python -c "from app.routers.admin.leads import router; from app.routers.admin.analytics import router as ar; print('OK')"` — backend imports valid
4. Manual: Overview page should show 4 sections (not 6), tag chips visible, click card enriched
</verification>

<success_criteria>
- Backend /leads returns click_count per lead
- Backend /analytics-summary returns active_tags per recent search entry
- Overview page has exactly 4 stat cards and 4 detail cards (Top Feedback and Top Searched removed)
- Tag-only search entries render as tag chips, not blank rows
- Text and tag search entries are visually distinguishable (different icon/label)
- Empty states show context-specific messages
</success_criteria>

<output>
After completion, create `.planning/phases/58.1-admin-dashboard-improvements/58.1-01-SUMMARY.md`
</output>
