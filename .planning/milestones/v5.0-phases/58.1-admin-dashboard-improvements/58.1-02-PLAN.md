---
phase: 58.1-admin-dashboard-improvements
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/admin/pages/DataPage.tsx
  - frontend/src/admin/pages/SearchesPage.tsx
  - frontend/src/admin/pages/AdminMarketplacePage.tsx
  - frontend/src/main.tsx
autonomous: true
requirements:
  - ADMUI-03

must_haves:
  truths:
    - "Admin navigating to /admin/data sees a single unified page with search records and marketplace trend/demand data — no tabs"
    - "A unified date range picker at the top of the Data page controls both the search records table filter and the trend chart period"
    - "Navigating to /admin/data/searches or /admin/data/marketplace redirects to /admin/data"
    - "All internal links that previously pointed to /admin/data/searches or /admin/data/marketplace now point to /admin/data"
  artifacts:
    - path: "frontend/src/admin/pages/DataPage.tsx"
      provides: "Merged single-page Data layout with unified date picker, search records table, and marketplace sections"
    - path: "frontend/src/main.tsx"
      provides: "Flattened /admin/data route with no child routes (or redirect children)"
  key_links:
    - from: "frontend/src/admin/pages/DataPage.tsx"
      to: "frontend/src/admin/pages/SearchesPage.tsx"
      via: "Inlined content or imported as component"
      pattern: "SearchesPage|SearchesTable"
    - from: "frontend/src/admin/pages/DataPage.tsx"
      to: "frontend/src/admin/pages/AdminMarketplacePage.tsx"
      via: "Inlined content or imported as component (TrendSection, DemandSection)"
      pattern: "TrendSection|DemandSection"
    - from: "frontend/src/admin/pages/OverviewPage.tsx"
      to: "/admin/data"
      via: "Updated link targets"
      pattern: "to=\"/admin/data\""
---

<objective>
Merge the Searches and Marketplace tabs into a single unified Data page with a shared date range picker.

Purpose: The current Data page has two tabs (Searches and Marketplace) that the user considers unnecessary scope separation — "there should be no difference between searches and marketplace." Merging them into one page simplifies navigation and enables a unified date filter that controls both the search records table and the trend chart.

Output: A single /admin/data page with no child routes, containing all search + marketplace data with a unified date filter.
</objective>

<execution_context>
@/Users/sebastianhamers/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sebastianhamers/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58.1-admin-dashboard-improvements/58.1-RESEARCH.md

@frontend/src/admin/pages/DataPage.tsx
@frontend/src/admin/pages/SearchesPage.tsx
@frontend/src/admin/pages/AdminMarketplacePage.tsx
@frontend/src/main.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge Searches and Marketplace content into unified DataPage</name>
  <files>
    frontend/src/admin/pages/DataPage.tsx
    frontend/src/admin/pages/SearchesPage.tsx
    frontend/src/admin/pages/AdminMarketplacePage.tsx
  </files>
  <action>
    Read all three page files fully before making changes.

    **Merge DataPage.tsx into a single-page layout:**
    1. Remove the tab bar navigation (Searches / Marketplace tabs) from `DataPage.tsx`
    2. Remove the `<Outlet />` pattern — DataPage becomes a full page component, not a layout wrapper

    **Add unified date range picker at the top:**
    1. Add date state at the DataPage level: `dateFrom` and `dateTo` (using native `<input type="date">` — same pattern as SearchesPage currently uses)
    2. The date picker should sit at the top of the page in an AdminCard or AdminPageHeader area
    3. Add preset buttons for common ranges: "Last 7 days", "Last 30 days", "All time" (similar to Overview period toggle but with explicit date inputs)

    **Inline SearchesPage content:**
    1. Move the search records table content from `SearchesPage.tsx` into DataPage
    2. Either import the SearchesTable component directly, or inline the relevant content
    3. Wire `dateFrom`/`dateTo` from the parent DataPage state to the search table's filter logic
    4. If SearchesPage has its own local date state, remove it and use the lifted parent state instead
    5. Keep the search filter input for filtering by query text (that's separate from date range)

    **Inline AdminMarketplacePage content:**
    1. Move TrendSection, DemandSection, and ExposureSection content into DataPage below the search records
    2. Wire the unified date range to TrendSection — currently TrendSection uses a hardcoded 14-day period. It should now use the parent DataPage's `dateFrom`/`dateTo` to determine the trend period
    3. If the trend chart API (`/events/trend`) accepts `days` param, convert the date range to a days count. If it accepts `from`/`to`, pass them directly. Read the backend endpoint to determine.
    4. Keep DemandSection and ExposureSection as-is below TrendSection (they show aggregate data not time-filtered)

    **Layout:**
    - Use AdminPageHeader with "Data" title
    - Unified date picker row below the header
    - Search records table (in AdminCard) — takes full width
    - Horizontal divider or spacing
    - Trend chart (in AdminCard) — takes full width
    - Demand + Exposure sections side by side (in AdminCards) — 2-column grid on lg

    **Empty states:**
    - Search records: "No searches found in this period" when table is empty
    - Trend chart: "No search activity in this period" when no data points

    **SearchesPage.tsx and AdminMarketplacePage.tsx:**
    - After moving their content into DataPage, these files can either be deleted or kept as thin wrappers. Preferred: keep them as component exports if DataPage imports from them, or delete if all content is inlined. Document the choice.
  </action>
  <verify>
    <automated>cd /Users/sebastianhamers/Documents/TCS/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Verify DataPage renders search table and marketplace sections in one view with a shared date picker</manual>
  </verify>
  <done>
    - DataPage.tsx is a single-page layout with no tabs or Outlet
    - Unified date range picker controls both search table and trend chart
    - Search records table and trend/demand/exposure sections visible on one page
    - Empty states present for all data sections
  </done>
</task>

<task type="auto">
  <name>Task 2: Update routes and fix all internal links</name>
  <files>
    frontend/src/main.tsx
    frontend/src/admin/pages/DataPage.tsx
  </files>
  <action>
    **Update route structure in `main.tsx`:**
    1. Find the `/admin/data` route and its children (`/admin/data/searches`, `/admin/data/marketplace`)
    2. Remove the child routes — DataPage is now a leaf route, not a layout
    3. Add redirect routes for `/admin/data/searches` and `/admin/data/marketplace` that redirect to `/admin/data`:
       ```tsx
       { path: "data/searches", element: <Navigate to="/admin/data" replace /> },
       { path: "data/marketplace", element: <Navigate to="/admin/data" replace /> },
       ```
    4. Import `Navigate` from `react-router-dom` if not already imported

    **Fix internal links (from RESEARCH.md pitfall analysis):**
    1. In `OverviewPage.tsx`: Update link at line ~178 from `to="/admin/data/searches"` to `to="/admin/data"`
    2. In `OverviewPage.tsx`: Update link at line ~212 from `to="/admin/data/marketplace"` to `to="/admin/data"`
    3. Grep the entire `frontend/src/admin/` directory for any other references to `/admin/data/searches` or `/admin/data/marketplace` and update them to `/admin/data`
    4. Check `AdminSidebar.tsx` — the sidebar nav item for "Data" should already point to `/admin/data`. Verify it doesn't point to a child route.
    5. Check stat card `onClick` handlers in OverviewPage that use `navigate('/admin/data/searches')` — update to `navigate('/admin/data')`

    **Clean up lazy imports in main.tsx:**
    - If SearchesPage and AdminMarketplacePage are still imported via React.lazy for child routes, remove those imports (they're no longer route components)
    - If they're imported by DataPage as components, keep the imports in DataPage
  </action>
  <verify>
    <automated>cd /Users/sebastianhamers/Documents/TCS/frontend && npx tsc --noEmit 2>&1 | head -30 && cd /Users/sebastianhamers/Documents/TCS && grep -rn "data/searches\|data/marketplace" frontend/src/admin/ --include="*.tsx" --include="*.ts" | grep -v "Navigate\|redirect\|PLAN\|RESEARCH\|SUMMARY" | head -20</automated>
    <manual>Verify no stale links to /admin/data/searches or /admin/data/marketplace exist</manual>
  </verify>
  <done>
    - /admin/data renders the merged DataPage directly (no child routes)
    - /admin/data/searches and /admin/data/marketplace redirect to /admin/data
    - All internal links and navigate() calls updated to /admin/data
    - No stale references to old child routes anywhere in admin code
  </done>
</task>

</tasks>

<verification>
1. `cd /Users/sebastianhamers/Documents/TCS/frontend && npx tsc --noEmit` — TypeScript compiles without errors
2. `cd /Users/sebastianhamers/Documents/TCS/frontend && npm run test` — existing tests pass
3. `grep -rn "data/searches\|data/marketplace" frontend/src/admin/ --include="*.tsx" --include="*.ts"` — only redirect routes should match, no stale links
4. Manual: Navigate to /admin/data — see unified page. Navigate to /admin/data/searches — redirects to /admin/data.
</verification>

<success_criteria>
- /admin/data shows a single page with search records table + marketplace trend/demand/exposure
- Unified date range picker controls both search table filters and trend chart period
- No tab bar or Outlet pattern in DataPage
- /admin/data/searches and /admin/data/marketplace redirect to /admin/data
- All internal links updated — no 404s from stale routes
- Empty state messages present on data sections
</success_criteria>

<output>
After completion, create `.planning/phases/58.1-admin-dashboard-improvements/58.1-02-SUMMARY.md`
</output>
