---
phase: 69.1-csv-upload-sync-remove-deleted-experts-and-update-existing-fields
plan: 03
subsystem: ui
tags: [react, tailwind, csv-sync, admin, expert-management]

# Dependency graph
requires:
  - POST /experts/sync-preview endpoint with to_add/to_update/to_delete + per-field diffs (from 69.1-02)
  - POST /experts/sync-apply endpoint with skip_deletes cherry-pick support (from 69.1-02)
provides:
  - Full sync UI: upload -> preview rows -> map columns -> analyze -> cherry-pick -> apply -> results
  - CsvImportModal with collapsible add/update/delete sections and per-expert deletion checkboxes
affects: []

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Collapsible Section component pattern: header with count badge, chevron toggle, defaultOpen driven by count
    - Cherry-pick deletion: Set<string> skipDeletes state, toggled per-username, sent as JSON array in FormData
    - Two-phase sync flow: preview step (read-only analysis) separated from apply step (write with skip_deletes)
    - Field diff display: old value strikethrough -> new value inline per changed attribute

key-files:
  created: []
  modified:
    - frontend/src/admin/components/CsvImportModal.tsx

key-decisions:
  - "Kept old import-csv/preview-csv flow intact — CsvImportModal now routes through sync-preview/sync-apply exclusively, but old endpoints still exist on backend for backward compat"
  - "Section component defined inside the file (not extracted) — only used in one place, keeps the modal self-contained"
  - "auto_advance: true — Task 2 human-verify checkpoint auto-approved per config"

patterns-established:
  - "Sync modal step sequence: idle -> preview -> mapping -> syncing-preview -> sync-review -> applying -> done"
  - "skipDeletes Set pattern: Set<string> state, toggleSkipDelete(username) adds/removes, serialized via Array.from() into FormData"

requirements-completed: []

# Metrics
duration: 2min
completed: 2026-03-04
---

# Phase 69.1 Plan 03: CSV Sync Preview UI Summary

**React modal with collapsible sync-preview sections (adds/updates/deletes), per-expert deletion checkboxes for cherry-pick control, field-level diff display, and a four-counter results screen**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-04T18:43:31Z
- **Completed:** 2026-03-04T18:45:33Z
- **Tasks:** 2 (1 auto + 1 auto-approved checkpoint)
- **Files modified:** 1

## Accomplishments

- Replaced old single-step `handleImport()` with two-phase flow: `handleSyncPreview()` calls `POST /experts/sync-preview` for read-only analysis, then `handleSyncApply()` calls `POST /experts/sync-apply` with `skip_deletes`
- Built collapsible Section component with green (adds), blue (updates), red (deletions) accent coloring and per-expert checkbox cherry-pick control
- Updates section shows per-field diffs: old value (strikethrough) -> new value inline; reactivating experts get a green "Reactivating" badge
- Summary bar at bottom of sync-review adjusts removal count live as admin toggles checkboxes, shows "(N experts kept)" callout
- Done step shows four stat cards: added, updated, removed, reactivated — plus FAISS rebuild indicator
- Added `syncing-preview` ("Analyzing sync changes...") and `applying` ("Applying sync...") spinner steps

## Task Commits

Each task was committed atomically:

1. **Task 1: Redesign CsvImportModal with sync preview and cherry-pick UI** — `8c0a1cf` (feat)
2. **Task 2: Verify full CSV sync flow end-to-end** — auto-approved (checkpoint:human-verify, auto_advance=true)

**Plan metadata:** (docs commit — this file)

## Files Created/Modified

- `frontend/src/admin/components/CsvImportModal.tsx` — Full rewrite: 352 lines -> 504 lines. New Step type, SyncPlan/SyncExpertAdd/Update/Delete/ImportResult interfaces, Section component, handleSyncPreview, handleSyncApply, toggleSkipDelete, full sync-review JSX with collapsible sections.

## Decisions Made

- Section component kept inline (not extracted to a separate file) — it's only used within CsvImportModal and the self-contained approach matches the existing modal pattern.
- `auto_advance: true` in config.json — Task 2 human-verify checkpoint was auto-approved per the plan's defined auto-advance behavior.

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- Phase 69.1 feature complete: soft-delete model (plan 01), sync endpoints (plan 02), and sync UI (plan 03) are all done
- Admin can upload a CSV, see a categorized preview of changes, deselect specific experts from deletion, and apply the sync with full result feedback
- FAISS rebuild fires automatically after sync-apply — search results will update in the background

---

## Self-Check

- [x] `frontend/src/admin/components/CsvImportModal.tsx` exists and is 504 lines
- [x] Commit `8c0a1cf` exists in git log
- [x] 16 occurrences of sync-preview/sync-apply/sync-review/skipDeletes/toggleSkipDelete confirmed
- [x] TypeScript check passes with no errors (npx tsc --noEmit)

## Self-Check: PASSED
