---
phase: 53-card-mobile-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/marketplace/ExpertCard.tsx
  - frontend/src/components/marketplace/ExpertGrid.tsx
  - frontend/src/components/marketplace/ExpertList.tsx
autonomous: true
requirements:
  - CARD-01
  - CARD-02
  - CARD-03

must_haves:
  truths:
    - "On mobile (<768px), each expert card shows a large profile photo (~40-50% of card height) with the expert name centered below it, followed by job title and hourly rate"
    - "On mobile, cards show no tags, match reason, or company — only photo, name, title, and rate"
    - "On mobile, tapping a card navigates directly (newsletter gate → profile) with no intermediate expand state"
    - "On desktop (>=768px), each expert card shows a larger profile photo on the left with name, title, company, rate, tags, match reason, and View Profile to its right"
    - "Desktop cards keep the existing hover animation (lift + purple glow)"
    - "Grid/list view toggle still works — list view also uses the new bigger-photo-left layout"
    - "Monogram initials fallback still works for experts without photos on both mobile and desktop"
  artifacts:
    - path: "frontend/src/components/marketplace/ExpertCard.tsx"
      provides: "Responsive expert card with mobile-first photo-centric layout and desktop photo-left layout"
      contains: "handleCardClick"
    - path: "frontend/src/components/marketplace/ExpertGrid.tsx"
      provides: "Grid view rendering cards without expand state management"
      contains: "VirtuosoGrid"
    - path: "frontend/src/components/marketplace/ExpertList.tsx"
      provides: "List view with bigger photo-left layout"
      contains: "Virtuoso"
  key_links:
    - from: "frontend/src/components/marketplace/ExpertCard.tsx"
      to: "onViewProfile callback"
      via: "handleCardClick always calls onViewProfile on mobile (no expand gate)"
      pattern: "onViewProfile\\(expert\\.profile_url\\)"
    - from: "frontend/src/components/marketplace/ExpertGrid.tsx"
      to: "frontend/src/components/marketplace/ExpertCard.tsx"
      via: "VirtuosoGrid itemContent renders ExpertCard without isExpanded/onExpand props"
      pattern: "ExpertCard"
---

<objective>
Redesign expert cards for mobile (photo-centric, centered name below, direct tap) and desktop (bigger photo on left, info inline right). Remove the two-tap expand behavior on mobile.

Purpose: Mobile cards currently show a tiny 32px photo with text-heavy layout and require two taps to reach a profile. The redesign makes cards visually clean and photo-forward on mobile, while desktop cards get a bigger photo without losing information density.

Output: Rewritten ExpertCard.tsx with responsive mobile/desktop layouts, updated ExpertGrid.tsx without expand state, updated ExpertList.tsx with bigger-photo-left layout.
</objective>

<execution_context>
@/Users/sebastianhamers/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sebastianhamers/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-card-mobile-redesign/53-CONTEXT.md
@frontend/src/components/marketplace/ExpertCard.tsx
@frontend/src/components/marketplace/ExpertGrid.tsx
@frontend/src/components/marketplace/ExpertList.tsx
@frontend/src/store/filterSlice.ts
@frontend/src/store/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign ExpertCard with responsive mobile/desktop layouts and remove tap-expand</name>
  <files>frontend/src/components/marketplace/ExpertCard.tsx</files>
  <action>
Rewrite ExpertCard.tsx with two distinct responsive layouts using Tailwind breakpoints (md: = 768px):

**Mobile layout (<768px):**
- Card uses a vertical column layout (flex-col, items-center)
- Profile photo: larger size (~80-96px circle), centered at top of card, taking ~40-50% of card height
- Monogram initials fallback: same large size with bg-brand-purple/10 text-brand-purple font-semibold
- Below photo: expert name (centered, font-semibold, text-sm), job title (centered, text-xs, text-gray-500), hourly rate (centered, text-xs font-semibold text-brand-purple)
- NO tags, NO match reason, NO company name, NO "View Full Profile" text
- Keep bookmark icon — position it as absolute top-right corner of the card
- Remove ALL expand/collapse logic for mobile: handleCardClick on mobile ALWAYS fires onViewProfile directly (no isExpanded check, no onExpand call)
- Keep the lead-click tracking and card_click event tracking

**Desktop layout (>=768px):**
- Card uses horizontal layout (flex-row)
- Profile photo on the left: larger (~64-72px circle), shrink-0
- Monogram fallback at same size
- Right side (flex-1, min-w-0): name (font-semibold, text-sm), job title (text-xs), company (text-xs text-gray-400), rate + findability badge, domain tag pills (first 2), match reason (line-clamp-2), "View Full Profile →" at bottom
- Keep current hover animation: add `hover:shadow-lg hover:shadow-brand-purple/10 hover:-translate-y-0.5` (the existing lift + purple glow pattern)
- Bookmark icon at top-right of card

**Both layouts:**
- handleCardClick: always call onViewProfile directly — remove the mobile two-tap expand branching entirely. Remove the isExpanded and onExpand props from the interface.
- Keep findabilityLabel function unchanged
- Keep _fireLeadClick function unchanged
- Keep trackEvent card_click tracking unchanged
- Card fixed height on mobile (~200px), overflow-hidden. On desktop, keep current h-[180px] or adjust to fit photo-left layout.

**Props cleanup:**
- Remove `isExpanded` prop (default was false)
- Remove `onExpand` prop
- Keep: expert, onViewProfile, context, rank
  </action>
  <verify>
    <automated>cd /Users/sebastianhamers/Documents/TCS && npx tsc --noEmit --project frontend/tsconfig.json 2>&1 | head -30</automated>
    <manual>Check ExpertCard.tsx has no isExpanded/onExpand props, has responsive md: breakpoint classes, mobile layout is vertical with large centered photo, desktop layout is horizontal with photo-left</manual>
  </verify>
  <done>ExpertCard renders a photo-centric centered mobile layout and a photo-left desktop layout. Tapping on mobile navigates directly with no expand step. Bookmark icon present on both layouts. Monogram fallback works at larger sizes.</done>
</task>

<task type="auto">
  <name>Task 2: Update ExpertGrid and ExpertList to use redesigned card without expand state</name>
  <files>
    frontend/src/components/marketplace/ExpertGrid.tsx
    frontend/src/components/marketplace/ExpertList.tsx
  </files>
  <action>
**ExpertGrid.tsx changes:**
- Remove the `expandedExpertId` state and `handleExpand` function entirely
- Remove the `isExpanded` and `onExpand` props from ExpertCard rendering inside VirtuosoGrid itemContent
- Keep all other logic: VirtuosoGrid, displayExperts memo, savedFilter, error handling, skeleton, empty state, spin animation
- Keep the grid class: `grid grid-cols-2 md:grid-cols-3 gap-4 px-4 pb-4 pt-4` — 2 columns mobile, 3 columns desktop (per user decision: Claude decides cards per row, 3 fits photo-left layout well on desktop)
- Remove the `useState` import for expandedExpertId if no other state remains

**ExpertList.tsx changes:**
- Update the list row layout to use a bigger photo on the left:
  - Photo size: increase from w-8 h-8 to w-12 h-12 (48px) or w-14 h-14 (56px) — make it visually prominent
  - Keep the monogram fallback at the same larger size
  - Keep name, job title, rate, and domain tags
  - Adjust spacing (gap-3 → gap-4) to accommodate larger photo
- The list view already navigates directly on click (no expand behavior) — no change needed there
  </action>
  <verify>
    <automated>cd /Users/sebastianhamers/Documents/TCS && npx tsc --noEmit --project frontend/tsconfig.json 2>&1 | head -30</automated>
    <manual>Verify ExpertGrid.tsx has no expandedExpertId state. Verify ExpertList.tsx has larger photo dimensions (w-12/w-14). Verify no TypeScript errors.</manual>
  </verify>
  <done>ExpertGrid renders cards without expand state management. ExpertList renders rows with bigger profile photos. Both compile without errors. Grid remains 2-col mobile / 3-col desktop.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit --project frontend/tsconfig.json` passes with no errors
2. `npx vite build` in frontend/ completes successfully
3. ExpertCard.tsx: no references to `isExpanded` or `onExpand`
4. ExpertCard.tsx: mobile layout shows large centered photo with name/title/rate below, no tags/company/match reason
5. ExpertCard.tsx: desktop layout shows photo-left with full info right
6. ExpertGrid.tsx: no `expandedExpertId` state, no `handleExpand` function
7. ExpertList.tsx: photo size is larger than previous 32px (w-8 h-8)
</verification>

<success_criteria>
- Expert cards on mobile are photo-forward with name centered below, no expand behavior
- Expert cards on desktop show bigger photo on left with full info to the right
- Grid/list views both work with the new card designs
- All TypeScript compilation passes
- No regressions in bookmark, tracking, or lead-click functionality
</success_criteria>

<output>
After completion, create `.planning/phases/53-card-mobile-redesign/53-01-SUMMARY.md`
</output>
