---
phase: 51-admin-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/routers/admin.py
  - frontend/src/admin/pages/OverviewPage.tsx
  - frontend/src/admin/hooks/useAdminData.ts
  - frontend/src/admin/types.ts
autonomous: true
requirements:
  - ADMN-01
  - ADMN-02

must_haves:
  truths:
    - "Admin overview page shows non-zero values for matches, searches, leads, lead rate, top searches, and gaps when data exists in the database"
    - "Clicking any stat card on the admin overview navigates to the corresponding detail page"
  artifacts:
    - path: "app/routers/admin.py"
      provides: "Fixed /stats endpoint returning correct non-zero data"
      contains: "get_stats"
    - path: "frontend/src/admin/pages/OverviewPage.tsx"
      provides: "Clickable stat cards with navigation"
      contains: "useNavigate"
  key_links:
    - from: "frontend/src/admin/pages/OverviewPage.tsx"
      to: "/api/admin/stats"
      via: "useAdminStats hook"
      pattern: "adminFetch.*stats"
    - from: "frontend/src/admin/pages/OverviewPage.tsx"
      to: "react-router-dom"
      via: "useNavigate + onClick handlers"
      pattern: "navigate.*admin"
---

<objective>
Fix admin overview stats that display zero and make stat cards clickable for navigation.

Purpose: The admin overview page currently shows all-zero values for key metrics (matches, searches, leads, lead rate, top searches, gaps) despite data existing in the database. Additionally, stat cards are static and don't navigate anywhere. This plan investigates and fixes the data pipeline bug, and adds click-to-navigate behavior on every stat card.

Output: Working overview page with accurate live stats and clickable navigation cards.
</objective>

<execution_context>
@/Users/sebastianhamers/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sebastianhamers/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-admin-fixes/51-CONTEXT.md
@app/routers/admin.py
@frontend/src/admin/pages/OverviewPage.tsx
@frontend/src/admin/hooks/useAdminData.ts
@frontend/src/admin/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Investigate and fix overview stats returning zero</name>
  <files>app/routers/admin.py, frontend/src/admin/pages/OverviewPage.tsx, frontend/src/admin/hooks/useAdminData.ts, frontend/src/admin/types.ts</files>
  <action>
The admin overview stats (matches, searches, leads, lead rate, top searches, gaps) all display zero. This is the primary bug to investigate and fix.

**Investigation approach — check these in order:**

1. **Backend /api/admin/stats endpoint** (`app/routers/admin.py`, `get_stats` function):
   - The endpoint queries `Conversation` table. Verify these queries return data when rows exist.
   - Check if `total_leads` counts from `Conversation.email` — this counts emails submitted via chat. The frontend also fetches from `/analytics-summary` endpoint which counts from `user_events` and `lead_clicks` tables. The overview page uses BOTH data sources. Check both.
   - Verify `top_queries` returns populated list (not empty).
   - Check if the `leads_7d` / `leads_prior_7d` 7-day trend queries use correct date math.

2. **Backend /api/admin/analytics-summary endpoint** (`get_analytics_summary` function):
   - Counts `card_click` and `search_query` events from `user_events` table — if no events are being tracked (e.g., tracking is broken or events use different event_type values), these will be zero.
   - Counts `lead_clicks` table rows.
   - Returns `recent_searches` and `recent_clicks` — check if these JSON extracts match the actual payload structure being stored.

3. **Frontend data display** (`OverviewPage.tsx`):
   - The page fetches from two hooks: `useAdminStats()` (calls /stats) and `useAnalyticsSummary()` (calls /analytics-summary).
   - The TrendStatCard rows use `stats.total_leads`, `stats.expert_pool`, `stats.top_queries`, `stats.lead_rate` — from /stats.
   - The StatCard rows in "Section 1" use `stats.total_searches`, `stats.match_count`, `stats.match_rate`, `stats.gap_count` — from /stats.
   - The "Section 1.5" cards use `analytics.total_card_clicks`, `analytics.total_search_queries`, `analytics.total_lead_clicks` — from /analytics-summary.
   - Check if TypeScript types in `types.ts` match the actual backend response shapes.

**Fix approach:**
- If the issue is that `Conversation` table data isn't being queried correctly (e.g., wrong column names, NULL comparisons), fix the SQLAlchemy queries.
- If the issue is that the `/analytics-summary` endpoint queries `user_events` with event types that don't match what's being written (e.g., event_type mismatch between tracking code and query code), fix the event type strings.
- If the issue is a frontend display bug (e.g., optional chaining silently returning 0, TypeScript types not matching), fix the display logic.
- If the data genuinely doesn't exist in some tables (e.g., `user_events` is empty because tracking was only recently added), that's not a bug but the display should show "No data yet" rather than 0.

**Important:** The "Matches" stat definition is at Claude's discretion per CONTEXT.md. Define "Matches" as the count of conversations where `response_type == 'match'` (already the case in `match_count`). Define "Lead Rate" as `total_leads / total_searches` (already the case in `lead_rate`). For the primary vs secondary lead metric: show email submissions as the main "Total Leads" number; profile clicks (from lead_clicks table) as "Lead Clicks" in the secondary section. This matches the current layout.

After investigation and fixes, verify the /stats endpoint returns non-zero values by running a quick curl test against the local backend (or reading the code to confirm the queries are correct).
  </action>
  <verify>
    <automated>cd /Users/sebastianhamers/Documents/TCS && python -c "
from app.database import SessionLocal
from app.models import Conversation, Expert, UserEvent, LeadClick
from sqlalchemy import func, select
db = SessionLocal()
convos = db.scalar(select(func.count()).select_from(Conversation)) or 0
experts = db.scalar(select(func.count()).select_from(Expert)) or 0
events = db.scalar(select(func.count()).select_from(UserEvent)) or 0
clicks = db.scalar(select(func.count()).select_from(LeadClick)) or 0
print(f'Conversations: {convos}, Experts: {experts}, Events: {events}, LeadClicks: {clicks}')
db.close()
print('DB check passed')
"</automated>
    <manual>Review the backend /stats and /analytics-summary endpoint code to confirm queries return non-zero data when tables have rows</manual>
  </verify>
  <done>The /api/admin/stats endpoint returns correct non-zero values for total_searches, match_count, match_rate, gap_count, total_leads, lead_rate, and top_queries when data exists in the database. The /api/admin/analytics-summary endpoint returns correct totals for card clicks, search queries, and lead clicks. The frontend OverviewPage correctly displays all values.</done>
</task>

<task type="auto">
  <name>Task 2: Make stat cards clickable with navigation to detail pages</name>
  <files>frontend/src/admin/pages/OverviewPage.tsx</files>
  <action>
Make every stat card on the admin overview page clickable, navigating to the appropriate detail page on click. Apply the navigation mapping from CONTEXT.md decisions:

- **Total Leads** card -> `/admin/leads`
- **Expert Pool** card -> `/admin/experts`
- **Total Searches** card -> `/admin/marketplace`
- **Top Searches** card -> `/admin/marketplace`
- **Gaps** card -> `/admin/marketplace`
- **Lead Rate** card -> `/admin/marketplace`
- **Matches** card -> `/admin/marketplace`
- **Match Rate** card -> `/admin/marketplace`
- **Expert Card Clicks** card -> `/admin/marketplace`
- **Explore Searches** card -> `/admin/marketplace`
- **Lead Clicks** card -> `/admin/leads`

**Implementation:**

1. Import `useNavigate` from `react-router-dom` in `OverviewPage.tsx`.

2. Update the `TrendStatCard` component to accept an optional `to` prop (string path) and an `onClick` handler:
   - Add `cursor-pointer hover:border-purple-500/40 hover:bg-slate-800/80 transition-all` classes when `to` is provided.
   - Wrap in a click handler that calls `navigate(to)`.

3. Update the `StatCard` component (the local one in OverviewPage, NOT the separate component file) to accept an optional `to` prop:
   - Same clickable styles: `cursor-pointer hover:border-purple-500/40 hover:bg-slate-800/80 transition-all`.
   - Wrap in a click handler that calls `navigate(to)`.

4. Pass the `to` prop to each card instance in the JSX:
   - Section 0 (TrendStatCard grid): Total Leads -> `/admin/leads`, Expert Pool -> `/admin/experts`, Top Searches -> `/admin/marketplace`, Lead Rate -> `/admin/marketplace`
   - Section 1 (StatCard grid): Total Searches -> `/admin/marketplace`, Matches -> `/admin/marketplace`, Match Rate -> `/admin/marketplace`, Gaps -> `/admin/marketplace`
   - Section 1.5 (analytics cards): Expert Card Clicks -> `/admin/marketplace`, Explore Searches -> `/admin/marketplace`, Lead Clicks -> `/admin/leads`

5. Ensure the clickable cards have visual affordance: `cursor-pointer` and a subtle hover state change so they feel interactive.

Do NOT modify the `StatCard` component in `frontend/src/admin/components/StatCard.tsx` — that's a separate unused/different component. Only modify the local `StatCard` and `TrendStatCard` functions defined within `OverviewPage.tsx`.
  </action>
  <verify>
    <automated>cd /Users/sebastianhamers/Documents/TCS/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>In the admin overview, hover over any stat card — cursor should change to pointer and card should show hover state. Click it and verify navigation to correct detail page.</manual>
  </verify>
  <done>Every stat card on the overview page is clickable with cursor-pointer and hover state. Clicking navigates to the correct detail page: Total Leads and Lead Clicks go to /admin/leads, Expert Pool goes to /admin/experts, all search/match/gap cards go to /admin/marketplace.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` — TypeScript compiles without errors
2. `cd frontend && npm run build` — Production build succeeds
3. Backend /stats endpoint returns non-zero data when DB has rows
4. Every stat card on overview navigates to the correct detail page
</verification>

<success_criteria>
- Admin overview page displays accurate non-zero stats when data exists in DB
- All stat cards are clickable with visual hover affordance
- Clicking each stat card navigates to the correct admin detail page
- No TypeScript errors, production build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/51-admin-fixes/51-01-SUMMARY.md`
</output>
