---
phase: 47-public-explorer-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/Header.tsx
  - frontend/src/hooks/useHeaderSearch.ts
autonomous: true
requirements:
  - EXP-01
  - EXP-02

must_haves:
  truths:
    - "Search bar input has a solid white background against the dark aurora header"
    - "Search bar has a thin light border with no shadow"
    - "Search bar placeholder reads 'Name, company, keyword...' (static, not cycling)"
  artifacts:
    - path: "frontend/src/components/Header.tsx"
      provides: "White background input with flat border styling"
      contains: "bg-white border border-slate-200"
    - path: "frontend/src/hooks/useHeaderSearch.ts"
      provides: "Single static placeholder text"
      contains: "Name, company, keyword..."
  key_links:
    - from: "frontend/src/hooks/useHeaderSearch.ts"
      to: "frontend/src/components/Header.tsx"
      via: "PLACEHOLDERS array consumed by animated overlay"
      pattern: "PLACEHOLDERS.*Name, company, keyword"
---

<objective>
Update the Explorer search bar to have a solid white background with a thin flat border (no shadow), and replace the cycling conversational placeholder phrases with a single static keyword-oriented placeholder: "Name, company, keyword..."

Purpose: The current semi-transparent input (`bg-white/50`) is hard to read against the dark aurora header background. The cycling conversational placeholders ("Who builds Stripe integrations?") are misleading — users should type keywords, not questions.

Output: Two small edits to existing files — no new components or state changes.
</objective>

<execution_context>
@/Users/sebastianhamers/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sebastianhamers/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/components/Header.tsx
@frontend/src/hooks/useHeaderSearch.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: White background input and static keyword placeholder</name>
  <files>frontend/src/components/Header.tsx, frontend/src/hooks/useHeaderSearch.ts</files>
  <action>
**Header.tsx — search input styling (line 183):**

Replace the input className to change from translucent to solid white with flat border:
- `bg-white/50` -> `bg-white`
- `border-slate-200/50` -> `border-slate-200`
- Remove `shadow-sm` (per locked decision: "no shadow — flat and clean")
- Keep `rounded-xl` (matches existing UI convention, per Claude's discretion)

The resulting className should be:
```
w-full pl-14 pr-8 py-2.5 rounded-xl text-sm bg-white border border-slate-200 focus:outline-none focus:ring-2 focus:ring-brand-purple/20 focus:border-brand-purple/40 transition-colors
```

**useHeaderSearch.ts — PLACEHOLDERS array (lines 9-18):**

Replace the entire PLACEHOLDERS array with a single entry:
```ts
const PLACEHOLDERS = [
  'Name, company, keyword...',
]
```

This preserves the AnimatePresence overlay machinery in Header.tsx without removing it (low risk). The overlay will display the single static placeholder when `localValue` is empty. The cycling animation still runs but with one item, so it stays visually static.

Do NOT change the `<input>` element's `placeholder` attribute — the visible placeholder is the animated overlay `<motion.span>`, not the HTML attribute.
  </action>
  <verify>
    <automated>cd /Users/sebastianhamers/Documents/TCS/frontend && npx vite build --mode production 2>&1 | tail -5</automated>
    <manual>Visually inspect: search bar should have pure white background, thin border, no shadow, placeholder reads "Name, company, keyword..."</manual>
  </verify>
  <done>Search input has bg-white class (not bg-white/50), border-slate-200 (not border-slate-200/50), no shadow-sm. PLACEHOLDERS array contains exactly one entry: "Name, company, keyword..."</done>
</task>

</tasks>

<verification>
1. `grep -n "bg-white " frontend/src/components/Header.tsx` shows the input line with `bg-white` (no /50 suffix)
2. `grep -n "shadow-sm" frontend/src/components/Header.tsx` returns no matches
3. `grep -n "PLACEHOLDERS" frontend/src/hooks/useHeaderSearch.ts` shows the array with single keyword entry
4. Vite production build succeeds without errors
</verification>

<success_criteria>
- Search input uses solid white background (`bg-white`, not `bg-white/50`)
- Border is `border-slate-200` (not `border-slate-200/50`)
- No `shadow-sm` on the input
- Placeholder text is "Name, company, keyword..." (single static entry)
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/47-public-explorer-polish/47-01-SUMMARY.md`
</output>
